/**
 * Dashboard — Business Command Center (§10.9).
 */
"use client";

import { CommandCenterDashboard } from "@/components/dashboard/command-center/command-center-dashboard";

export default function DashboardPage() {
  return <CommandCenterDashboard />;
}
