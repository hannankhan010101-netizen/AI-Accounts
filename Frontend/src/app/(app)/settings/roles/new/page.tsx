/** Create role — catalog §12.4 */
"use client";

import { RoleEditorForm } from "@/components/settings/role-editor-form";

export default function NewRolePage() {
  return <RoleEditorForm mode="create" />;
}
