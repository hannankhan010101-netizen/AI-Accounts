"use client";

import { CommandCenterDashboard } from "@/components/dashboard/command-center/command-center-dashboard";

/** @deprecated Use CommandCenterDashboard — kept for one release cycle. */
export function BusinessOverviewDashboard() {
  return <CommandCenterDashboard />;
}

export { CommandCenterDashboard };
