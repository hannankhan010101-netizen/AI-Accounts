/** Central brand asset paths and product naming. */
export const BRAND = {
  name: "AI-Accounts",
  tagline: "Enterprise-grade AI platform",
  /** Symbol mark is rendered inline via `BrandMark` (ledger + AI sparkle). */
  symbol: "inline",
  /** Full lockup with wordmark — print / legacy only. */
  logo: "/brand/logo.png",
} as const;
