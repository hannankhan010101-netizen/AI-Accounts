# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template models.py.jinja --
import os
import logging
import inspect
import warnings
from collections import OrderedDict

from pydantic import BaseModel, Field

from . import types, enums, errors, fields, bases
from ._types import FuncType
from ._compat import model_rebuild, field_validator
from ._builder import serialize_base64
from .generator import partial_models_ctx, PartialModelField


log: logging.Logger = logging.getLogger(__name__)
_created_partial_types: Set[str] = set()

class Company(bases.BaseCompany):
    """Represents a Company record"""

    id: _str
    name: _str
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    memberships: Optional[List['models.CompanyMembership']] = None
    roles: Optional[List['models.Role']] = None
    businessInformation: Optional['models.BusinessInformation'] = None
    smartSettings: Optional['models.SmartSettings'] = None
    lockDateSettings: Optional['models.LockDateSettings'] = None
    taxesYearEnd: Optional['models.TaxesYearEndConfig'] = None
    coaCategories: Optional[List['models.CoaCategory']] = None
    bankAccounts: Optional[List['models.BankAccount']] = None
    customers: Optional[List['models.Customer']] = None
    suppliers: Optional[List['models.Supplier']] = None
    products: Optional[List['models.Product']] = None
    projects: Optional[List['models.Project']] = None
    locations: Optional[List['models.Location']] = None
    journals: Optional[List['models.Journal']] = None
    attachments: Optional[List['models.Attachment']] = None
    importJobs: Optional[List['models.ImportJob']] = None
    auditLogEntries: Optional[List['models.AuditLogEntry']] = None
    documentSequences: Optional[List['models.DocumentNumberSequence']] = None
    approvalPolicies: Optional[List['models.ApprovalPolicy']] = None
    salesInvoices: Optional[List['models.SalesInvoice']] = None
    supplierBills: Optional[List['models.SupplierBill']] = None
    bankPayments: Optional[List['models.BankPayment']] = None
    salesReceipts: Optional[List['models.SalesReceipt']] = None
    supplierPayments: Optional[List['models.SupplierPayment']] = None
    bankReceipts: Optional[List['models.BankReceipt']] = None
    bankTransfers: Optional[List['models.BankTransfer']] = None
    quotations: Optional[List['models.Quotation']] = None
    salesOrders: Optional[List['models.SalesOrder']] = None
    purchaseOrders: Optional[List['models.PurchaseOrder']] = None
    salesCredits: Optional[List['models.SalesCredit']] = None
    supplierCredits: Optional[List['models.SupplierCredit']] = None
    stockAdjustments: Optional[List['models.StockAdjustment']] = None
    stockTransfers: Optional[List['models.StockTransfer']] = None
    productBatches: Optional[List['models.ProductBatch']] = None
    deliveryNotes: Optional[List['models.DeliveryNote']] = None
    goodsReceiptNotes: Optional[List['models.GoodsReceiptNote']] = None
    pdcReceived: Optional[List['models.PostDatedChequeReceived']] = None
    pdcIssued: Optional[List['models.PostDatedChequeIssued']] = None
    outboxEvents: Optional[List['models.OutboxEvent']] = None
    reportRuns: Optional[List['models.ReportRun']] = None
    bankReconciliations: Optional[List['models.BankReconciliation']] = None
    goodsIssues: Optional[List['models.GoodsIssue']] = None
    approvalRequests: Optional[List['models.ApprovalRequest']] = None
    assemblyTemplates: Optional[List['models.AssemblyTemplate']] = None
    assemblyJobs: Optional[List['models.AssemblyJob']] = None
    moduleEntitlements: Optional[List['models.CompanyModuleEntitlement']] = None
    customFieldDefinitions: Optional[List['models.CustomFieldDefinition']] = None
    subscriptionRecord: Optional['models.SubscriptionRecord'] = None
    fxRevaluationRuns: Optional[List['models.FxRevaluationRun']] = None
    digitalInvoiceSubmissions: Optional[List['models.DigitalInvoiceSubmission']] = None
    paymentGatewayTransactions: Optional[List['models.PaymentGatewayTransaction']] = None
    userOnboardingStates: Optional[List['models.UserOnboardingState']] = None
    onboardingReleaseItems: Optional[List['models.OnboardingReleaseItem']] = None
    assistantThreads: Optional[List['models.AssistantThread']] = None
    budgets: Optional[List['models.Budget']] = None
    transactionTemplates: Optional[List['models.TransactionTemplate']] = None
    recurringSchedules: Optional[List['models.RecurringSchedule']] = None
    customerAdvanceReturns: Optional[List['models.CustomerAdvanceReturn']] = None
    supplierAdvanceReturns: Optional[List['models.SupplierAdvanceReturn']] = None
    companyModuleConfigs: Optional[List['models.CompanyModuleConfig']] = None
    fieldSecurityPolicies: Optional[List['models.FieldSecurityPolicy']] = None
    userDataAssignments: Optional[List['models.UserDataAssignment']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CompanyKeys']] = None,
        exclude: Optional[Iterable['types.CompanyKeys']] = None,
        required: Optional[Iterable['types.CompanyKeys']] = None,
        optional: Optional[Iterable['types.CompanyKeys']] = None,
        relations: Optional[Mapping['types.CompanyRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CompanyKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Company_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Company_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Company_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Company_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Company_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Company_relational_fields:
                        raise errors.UnknownRelationalFieldError('Company', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Company / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Company',
            }
        )
        _created_partial_types.add(name)


class AssistantThread(bases.BaseAssistantThread):
    """AI copilot conversation thread â€” per user per company.
    """

    id: _str
    companyId: _str
    userId: _str
    locale: _str
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    company: Optional['models.Company'] = None
    user: Optional['models.User'] = None
    messages: Optional[List['models.AssistantMessage']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.AssistantThreadKeys']] = None,
        exclude: Optional[Iterable['types.AssistantThreadKeys']] = None,
        required: Optional[Iterable['types.AssistantThreadKeys']] = None,
        optional: Optional[Iterable['types.AssistantThreadKeys']] = None,
        relations: Optional[Mapping['types.AssistantThreadRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.AssistantThreadKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _AssistantThread_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _AssistantThread_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _AssistantThread_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _AssistantThread_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _AssistantThread_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _AssistantThread_relational_fields:
                        raise errors.UnknownRelationalFieldError('AssistantThread', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid AssistantThread / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'AssistantThread',
            }
        )
        _created_partial_types.add(name)


class AssistantMessage(bases.BaseAssistantMessage):
    """Represents a AssistantMessage record"""

    id: _str
    threadId: _str
    role: _str
    content: _str
    toolName: Optional[_str] = None
    toolPayload: Optional['fields.Json'] = None
    createdAt: datetime.datetime
    thread: Optional['models.AssistantThread'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.AssistantMessageKeys']] = None,
        exclude: Optional[Iterable['types.AssistantMessageKeys']] = None,
        required: Optional[Iterable['types.AssistantMessageKeys']] = None,
        optional: Optional[Iterable['types.AssistantMessageKeys']] = None,
        relations: Optional[Mapping['types.AssistantMessageRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.AssistantMessageKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _AssistantMessage_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _AssistantMessage_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _AssistantMessage_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _AssistantMessage_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _AssistantMessage_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _AssistantMessage_relational_fields:
                        raise errors.UnknownRelationalFieldError('AssistantMessage', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid AssistantMessage / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'AssistantMessage',
            }
        )
        _created_partial_types.add(name)


class PlatformOnboardingRelease(bases.BasePlatformOnboardingRelease):
    """Platform-wide What's New (all tenants) â€” P52.
    """

    id: _str
    releaseKey: _str
    version: _str
    title: _str
    summary: _str
    publishedAt: _str
    tourId: Optional[_str] = None
    href: Optional[_str] = None
    permissions: 'fields.Json'
    isActive: _bool
    sortOrder: _int
    createdAt: datetime.datetime
    updatedAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PlatformOnboardingReleaseKeys']] = None,
        exclude: Optional[Iterable['types.PlatformOnboardingReleaseKeys']] = None,
        required: Optional[Iterable['types.PlatformOnboardingReleaseKeys']] = None,
        optional: Optional[Iterable['types.PlatformOnboardingReleaseKeys']] = None,
        relations: Optional[Mapping['types.PlatformOnboardingReleaseRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PlatformOnboardingReleaseKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _PlatformOnboardingRelease_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _PlatformOnboardingRelease_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _PlatformOnboardingRelease_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _PlatformOnboardingRelease_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "PlatformOnboardingRelease" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid PlatformOnboardingRelease / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'PlatformOnboardingRelease',
            }
        )
        _created_partial_types.add(name)


class OnboardingReleaseItem(bases.BaseOnboardingReleaseItem):
    """Tenant-managed What's New entries (CMS) â€” P51. Merged with platform catalog.
    """

    id: _str
    companyId: _str
    releaseKey: _str
    version: _str
    title: _str
    summary: _str
    publishedAt: _str
    tourId: Optional[_str] = None
    href: Optional[_str] = None
    permissions: 'fields.Json'
    isActive: _bool
    sortOrder: _int
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.OnboardingReleaseItemKeys']] = None,
        exclude: Optional[Iterable['types.OnboardingReleaseItemKeys']] = None,
        required: Optional[Iterable['types.OnboardingReleaseItemKeys']] = None,
        optional: Optional[Iterable['types.OnboardingReleaseItemKeys']] = None,
        relations: Optional[Mapping['types.OnboardingReleaseItemRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.OnboardingReleaseItemKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _OnboardingReleaseItem_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _OnboardingReleaseItem_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _OnboardingReleaseItem_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _OnboardingReleaseItem_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _OnboardingReleaseItem_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _OnboardingReleaseItem_relational_fields:
                        raise errors.UnknownRelationalFieldError('OnboardingReleaseItem', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid OnboardingReleaseItem / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'OnboardingReleaseItem',
            }
        )
        _created_partial_types.add(name)


class UserOnboardingState(bases.BaseUserOnboardingState):
    """Per-user guided tour progress and analytics buffer â€” P48.
    """

    id: _str
    companyId: _str
    userId: _str
    payload: 'fields.Json'
    updatedAt: datetime.datetime
    company: Optional['models.Company'] = None
    user: Optional['models.User'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.UserOnboardingStateKeys']] = None,
        exclude: Optional[Iterable['types.UserOnboardingStateKeys']] = None,
        required: Optional[Iterable['types.UserOnboardingStateKeys']] = None,
        optional: Optional[Iterable['types.UserOnboardingStateKeys']] = None,
        relations: Optional[Mapping['types.UserOnboardingStateRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.UserOnboardingStateKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _UserOnboardingState_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _UserOnboardingState_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _UserOnboardingState_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _UserOnboardingState_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _UserOnboardingState_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _UserOnboardingState_relational_fields:
                        raise errors.UnknownRelationalFieldError('UserOnboardingState', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid UserOnboardingState / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'UserOnboardingState',
            }
        )
        _created_partial_types.add(name)


class User(bases.BaseUser):
    """Represents a User record"""

    id: _str
    email: _str
    passwordHash: _str
    firstName: _str
    lastName: _str
    phone: Optional[_str] = None
    isActive: _bool
    emailVerified: _bool
    """When false, user must complete ``signup`` email OTP before login.
    """

    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    memberships: Optional[List['models.CompanyMembership']] = None
    lockDatePerUserRows: Optional[List['models.LockDatePerUser']] = None
    otpChallenges: Optional[List['models.AuthOtpChallenge']] = None
    onboardingStates: Optional[List['models.UserOnboardingState']] = None
    assistantThreads: Optional[List['models.AssistantThread']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.UserKeys']] = None,
        exclude: Optional[Iterable['types.UserKeys']] = None,
        required: Optional[Iterable['types.UserKeys']] = None,
        optional: Optional[Iterable['types.UserKeys']] = None,
        relations: Optional[Mapping['types.UserRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.UserKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _User_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _User_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _User_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _User_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _User_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _User_relational_fields:
                        raise errors.UnknownRelationalFieldError('User', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid User / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'User',
            }
        )
        _created_partial_types.add(name)


class AuthOtpChallenge(bases.BaseAuthOtpChallenge):
    """One-time codes for email verification (signup) and password reset.
    """

    id: _str
    userId: _str
    purpose: _str
    codeHash: _str
    expiresAt: datetime.datetime
    createdAt: datetime.datetime
    user: Optional['models.User'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.AuthOtpChallengeKeys']] = None,
        exclude: Optional[Iterable['types.AuthOtpChallengeKeys']] = None,
        required: Optional[Iterable['types.AuthOtpChallengeKeys']] = None,
        optional: Optional[Iterable['types.AuthOtpChallengeKeys']] = None,
        relations: Optional[Mapping['types.AuthOtpChallengeRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.AuthOtpChallengeKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _AuthOtpChallenge_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _AuthOtpChallenge_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _AuthOtpChallenge_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _AuthOtpChallenge_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _AuthOtpChallenge_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _AuthOtpChallenge_relational_fields:
                        raise errors.UnknownRelationalFieldError('AuthOtpChallenge', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid AuthOtpChallenge / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'AuthOtpChallenge',
            }
        )
        _created_partial_types.add(name)


class CompanyMembership(bases.BaseCompanyMembership):
    """Represents a CompanyMembership record"""

    id: _str
    companyId: _str
    userId: _str
    roleId: Optional[_str] = None
    isDefault: _bool
    ipAllowlist: Optional[_str] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    user: Optional['models.User'] = None
    role: Optional['models.Role'] = None
    membershipRoles: Optional[List['models.MembershipRole']] = None
    dataAssignments: Optional[List['models.UserDataAssignment']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CompanyMembershipKeys']] = None,
        exclude: Optional[Iterable['types.CompanyMembershipKeys']] = None,
        required: Optional[Iterable['types.CompanyMembershipKeys']] = None,
        optional: Optional[Iterable['types.CompanyMembershipKeys']] = None,
        relations: Optional[Mapping['types.CompanyMembershipRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CompanyMembershipKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _CompanyMembership_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _CompanyMembership_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _CompanyMembership_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _CompanyMembership_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _CompanyMembership_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _CompanyMembership_relational_fields:
                        raise errors.UnknownRelationalFieldError('CompanyMembership', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid CompanyMembership / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'CompanyMembership',
            }
        )
        _created_partial_types.add(name)


class Role(bases.BaseRole):
    """Represents a Role record"""

    id: _str
    companyId: _str
    name: _str
    code: Optional[_str] = None
    description: Optional[_str] = None
    parentRoleId: Optional[_str] = None
    isSystem: _bool
    isTemplate: _bool
    sortOrder: _int
    permissions: 'fields.Json'
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    company: Optional['models.Company'] = None
    parentRole: Optional['models.Role'] = None
    childRoles: Optional[List['models.Role']] = None
    memberships: Optional[List['models.CompanyMembership']] = None
    membershipRoles: Optional[List['models.MembershipRole']] = None
    rolePermissions: Optional[List['models.RolePermission']] = None
    fieldSecurityPolicies: Optional[List['models.FieldSecurityPolicy']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.RoleKeys']] = None,
        exclude: Optional[Iterable['types.RoleKeys']] = None,
        required: Optional[Iterable['types.RoleKeys']] = None,
        optional: Optional[Iterable['types.RoleKeys']] = None,
        relations: Optional[Mapping['types.RoleRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.RoleKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Role_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Role_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Role_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Role_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Role_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Role_relational_fields:
                        raise errors.UnknownRelationalFieldError('Role', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Role / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Role',
            }
        )
        _created_partial_types.add(name)


class PermissionDefinition(bases.BasePermissionDefinition):
    """Normalized permission catalog (seeded from permission_registry).
    """

    code: _str
    module: _str
    resource: _str
    action: _str
    label: _str
    groupLabel: Optional[_str] = None
    sortOrder: _int
    rolePermissions: Optional[List['models.RolePermission']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PermissionDefinitionKeys']] = None,
        exclude: Optional[Iterable['types.PermissionDefinitionKeys']] = None,
        required: Optional[Iterable['types.PermissionDefinitionKeys']] = None,
        optional: Optional[Iterable['types.PermissionDefinitionKeys']] = None,
        relations: Optional[Mapping['types.PermissionDefinitionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PermissionDefinitionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _PermissionDefinition_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _PermissionDefinition_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _PermissionDefinition_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _PermissionDefinition_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _PermissionDefinition_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _PermissionDefinition_relational_fields:
                        raise errors.UnknownRelationalFieldError('PermissionDefinition', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid PermissionDefinition / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'PermissionDefinition',
            }
        )
        _created_partial_types.add(name)


class RolePermission(bases.BaseRolePermission):
    """Role â†” permission grants (replaces JSON over time).
    """

    roleId: _str
    permissionCode: _str
    granted: _bool
    role: Optional['models.Role'] = None
    permission: Optional['models.PermissionDefinition'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.RolePermissionKeys']] = None,
        exclude: Optional[Iterable['types.RolePermissionKeys']] = None,
        required: Optional[Iterable['types.RolePermissionKeys']] = None,
        optional: Optional[Iterable['types.RolePermissionKeys']] = None,
        relations: Optional[Mapping['types.RolePermissionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.RolePermissionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _RolePermission_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _RolePermission_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _RolePermission_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _RolePermission_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _RolePermission_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _RolePermission_relational_fields:
                        raise errors.UnknownRelationalFieldError('RolePermission', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid RolePermission / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'RolePermission',
            }
        )
        _created_partial_types.add(name)


class MembershipRole(bases.BaseMembershipRole):
    """Multi-role assignment per company membership.
    """

    membershipId: _str
    roleId: _str
    isPrimary: _bool
    membership: Optional['models.CompanyMembership'] = None
    role: Optional['models.Role'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.MembershipRoleKeys']] = None,
        exclude: Optional[Iterable['types.MembershipRoleKeys']] = None,
        required: Optional[Iterable['types.MembershipRoleKeys']] = None,
        optional: Optional[Iterable['types.MembershipRoleKeys']] = None,
        relations: Optional[Mapping['types.MembershipRoleRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.MembershipRoleKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _MembershipRole_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _MembershipRole_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _MembershipRole_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _MembershipRole_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _MembershipRole_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _MembershipRole_relational_fields:
                        raise errors.UnknownRelationalFieldError('MembershipRole', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid MembershipRole / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'MembershipRole',
            }
        )
        _created_partial_types.add(name)


class CompanyModuleConfig(bases.BaseCompanyModuleConfig):
    """Per-company module kill-switch and visibility (Access Control).
    """

    companyId: _str
    moduleCode: _str
    enabled: _bool
    sidebarVisible: _bool
    routesEnabled: _bool
    apiEnabled: _bool
    reportsEnabled: _bool
    widgetsEnabled: _bool
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CompanyModuleConfigKeys']] = None,
        exclude: Optional[Iterable['types.CompanyModuleConfigKeys']] = None,
        required: Optional[Iterable['types.CompanyModuleConfigKeys']] = None,
        optional: Optional[Iterable['types.CompanyModuleConfigKeys']] = None,
        relations: Optional[Mapping['types.CompanyModuleConfigRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CompanyModuleConfigKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _CompanyModuleConfig_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _CompanyModuleConfig_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _CompanyModuleConfig_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _CompanyModuleConfig_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _CompanyModuleConfig_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _CompanyModuleConfig_relational_fields:
                        raise errors.UnknownRelationalFieldError('CompanyModuleConfig', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid CompanyModuleConfig / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'CompanyModuleConfig',
            }
        )
        _created_partial_types.add(name)


class FieldSecurityPolicy(bases.BaseFieldSecurityPolicy):
    """Field-level security per role.
    """

    id: _str
    companyId: _str
    roleId: _str
    fieldKey: _str
    accessLevel: _str
    company: Optional['models.Company'] = None
    role: Optional['models.Role'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.FieldSecurityPolicyKeys']] = None,
        exclude: Optional[Iterable['types.FieldSecurityPolicyKeys']] = None,
        required: Optional[Iterable['types.FieldSecurityPolicyKeys']] = None,
        optional: Optional[Iterable['types.FieldSecurityPolicyKeys']] = None,
        relations: Optional[Mapping['types.FieldSecurityPolicyRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.FieldSecurityPolicyKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _FieldSecurityPolicy_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _FieldSecurityPolicy_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _FieldSecurityPolicy_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _FieldSecurityPolicy_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _FieldSecurityPolicy_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _FieldSecurityPolicy_relational_fields:
                        raise errors.UnknownRelationalFieldError('FieldSecurityPolicy', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid FieldSecurityPolicy / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'FieldSecurityPolicy',
            }
        )
        _created_partial_types.add(name)


class UserDataAssignment(bases.BaseUserDataAssignment):
    """Data scope restrictions per membership.
    """

    id: _str
    companyId: _str
    membershipId: _str
    scopeType: _str
    scopeId: _str
    company: Optional['models.Company'] = None
    membership: Optional['models.CompanyMembership'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.UserDataAssignmentKeys']] = None,
        exclude: Optional[Iterable['types.UserDataAssignmentKeys']] = None,
        required: Optional[Iterable['types.UserDataAssignmentKeys']] = None,
        optional: Optional[Iterable['types.UserDataAssignmentKeys']] = None,
        relations: Optional[Mapping['types.UserDataAssignmentRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.UserDataAssignmentKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _UserDataAssignment_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _UserDataAssignment_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _UserDataAssignment_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _UserDataAssignment_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _UserDataAssignment_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _UserDataAssignment_relational_fields:
                        raise errors.UnknownRelationalFieldError('UserDataAssignment', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid UserDataAssignment / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'UserDataAssignment',
            }
        )
        _created_partial_types.add(name)


class BusinessInformation(bases.BaseBusinessInformation):
    """Represents a BusinessInformation record"""

    id: _str
    companyId: _str
    businessName: Optional[_str] = None
    addressLine1: Optional[_str] = None
    addressLine2: Optional[_str] = None
    addressLine3: Optional[_str] = None
    addressLine4: Optional[_str] = None
    addressLine5: Optional[_str] = None
    branchName: Optional[_str] = None
    phoneNumber: Optional[_str] = None
    mobileNumber: Optional[_str] = None
    emailAddress: Optional[_str] = None
    websiteAddress: Optional[_str] = None
    logoUrl: Optional[_str] = None
    cnic: Optional[_str] = None
    stn: Optional[_str] = None
    ntn: Optional[_str] = None
    applyOnAllPrints: _bool
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.BusinessInformationKeys']] = None,
        exclude: Optional[Iterable['types.BusinessInformationKeys']] = None,
        required: Optional[Iterable['types.BusinessInformationKeys']] = None,
        optional: Optional[Iterable['types.BusinessInformationKeys']] = None,
        relations: Optional[Mapping['types.BusinessInformationRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.BusinessInformationKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _BusinessInformation_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _BusinessInformation_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _BusinessInformation_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _BusinessInformation_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _BusinessInformation_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _BusinessInformation_relational_fields:
                        raise errors.UnknownRelationalFieldError('BusinessInformation', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid BusinessInformation / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'BusinessInformation',
            }
        )
        _created_partial_types.add(name)


class SmartSettings(bases.BaseSmartSettings):
    """Represents a SmartSettings record"""

    id: _str
    companyId: _str
    payload: 'fields.Json'
    updatedAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SmartSettingsKeys']] = None,
        exclude: Optional[Iterable['types.SmartSettingsKeys']] = None,
        required: Optional[Iterable['types.SmartSettingsKeys']] = None,
        optional: Optional[Iterable['types.SmartSettingsKeys']] = None,
        relations: Optional[Mapping['types.SmartSettingsRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SmartSettingsKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SmartSettings_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SmartSettings_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SmartSettings_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SmartSettings_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SmartSettings_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SmartSettings_relational_fields:
                        raise errors.UnknownRelationalFieldError('SmartSettings', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SmartSettings / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SmartSettings',
            }
        )
        _created_partial_types.add(name)


class LockDateSettings(bases.BaseLockDateSettings):
    """Represents a LockDateSettings record"""

    id: _str
    companyId: _str
    globalLockDate: Optional[datetime.datetime] = None
    updatedAt: datetime.datetime
    company: Optional['models.Company'] = None
    perUserExtensions: Optional[List['models.LockDatePerUser']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.LockDateSettingsKeys']] = None,
        exclude: Optional[Iterable['types.LockDateSettingsKeys']] = None,
        required: Optional[Iterable['types.LockDateSettingsKeys']] = None,
        optional: Optional[Iterable['types.LockDateSettingsKeys']] = None,
        relations: Optional[Mapping['types.LockDateSettingsRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.LockDateSettingsKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _LockDateSettings_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _LockDateSettings_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _LockDateSettings_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _LockDateSettings_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _LockDateSettings_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _LockDateSettings_relational_fields:
                        raise errors.UnknownRelationalFieldError('LockDateSettings', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid LockDateSettings / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'LockDateSettings',
            }
        )
        _created_partial_types.add(name)


class LockDatePerUser(bases.BaseLockDatePerUser):
    """Represents a LockDatePerUser record"""

    id: _str
    lockDateSettingsId: _str
    userId: _str
    extendedLockDate: datetime.datetime
    lockDateSettings: Optional['models.LockDateSettings'] = None
    user: Optional['models.User'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.LockDatePerUserKeys']] = None,
        exclude: Optional[Iterable['types.LockDatePerUserKeys']] = None,
        required: Optional[Iterable['types.LockDatePerUserKeys']] = None,
        optional: Optional[Iterable['types.LockDatePerUserKeys']] = None,
        relations: Optional[Mapping['types.LockDatePerUserRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.LockDatePerUserKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _LockDatePerUser_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _LockDatePerUser_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _LockDatePerUser_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _LockDatePerUser_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _LockDatePerUser_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _LockDatePerUser_relational_fields:
                        raise errors.UnknownRelationalFieldError('LockDatePerUser', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid LockDatePerUser / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'LockDatePerUser',
            }
        )
        _created_partial_types.add(name)


class TaxesYearEndConfig(bases.BaseTaxesYearEndConfig):
    """Represents a TaxesYearEndConfig record"""

    id: _str
    companyId: _str
    yearEndDate: Optional[datetime.datetime] = None
    taxDisplay: 'fields.Json'
    gstRates: 'fields.Json'
    fedRates: 'fields.Json'
    adtRates: 'fields.Json'
    whtRates: 'fields.Json'
    taxRegions: 'fields.Json'
    updatedAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.TaxesYearEndConfigKeys']] = None,
        exclude: Optional[Iterable['types.TaxesYearEndConfigKeys']] = None,
        required: Optional[Iterable['types.TaxesYearEndConfigKeys']] = None,
        optional: Optional[Iterable['types.TaxesYearEndConfigKeys']] = None,
        relations: Optional[Mapping['types.TaxesYearEndConfigRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.TaxesYearEndConfigKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _TaxesYearEndConfig_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _TaxesYearEndConfig_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _TaxesYearEndConfig_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _TaxesYearEndConfig_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _TaxesYearEndConfig_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _TaxesYearEndConfig_relational_fields:
                        raise errors.UnknownRelationalFieldError('TaxesYearEndConfig', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid TaxesYearEndConfig / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'TaxesYearEndConfig',
            }
        )
        _created_partial_types.add(name)


class CoaCategory(bases.BaseCoaCategory):
    """Represents a CoaCategory record"""

    id: _str
    companyId: _str
    code: _str
    name: _str
    sortOrder: _int
    categoryType: _str
    """Income / Expense / Asset / Liability / Equity / Other â€” drives P&L vs Balance Sheet classification.
    """

    company: Optional['models.Company'] = None
    sections: Optional[List['models.CoaSection']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CoaCategoryKeys']] = None,
        exclude: Optional[Iterable['types.CoaCategoryKeys']] = None,
        required: Optional[Iterable['types.CoaCategoryKeys']] = None,
        optional: Optional[Iterable['types.CoaCategoryKeys']] = None,
        relations: Optional[Mapping['types.CoaCategoryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CoaCategoryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _CoaCategory_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _CoaCategory_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _CoaCategory_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _CoaCategory_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _CoaCategory_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _CoaCategory_relational_fields:
                        raise errors.UnknownRelationalFieldError('CoaCategory', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid CoaCategory / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'CoaCategory',
            }
        )
        _created_partial_types.add(name)


class CoaSection(bases.BaseCoaSection):
    """Represents a CoaSection record"""

    id: _str
    categoryId: _str
    code: _str
    name: _str
    sortOrder: _int
    category: Optional['models.CoaCategory'] = None
    nominals: Optional[List['models.NominalAccount']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CoaSectionKeys']] = None,
        exclude: Optional[Iterable['types.CoaSectionKeys']] = None,
        required: Optional[Iterable['types.CoaSectionKeys']] = None,
        optional: Optional[Iterable['types.CoaSectionKeys']] = None,
        relations: Optional[Mapping['types.CoaSectionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CoaSectionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _CoaSection_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _CoaSection_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _CoaSection_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _CoaSection_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _CoaSection_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _CoaSection_relational_fields:
                        raise errors.UnknownRelationalFieldError('CoaSection', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid CoaSection / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'CoaSection',
            }
        )
        _created_partial_types.add(name)


class NominalAccount(bases.BaseNominalAccount):
    """Represents a NominalAccount record"""

    id: _str
    sectionId: _str
    code: _str
    name: _str
    description: Optional[_str] = None
    isChargeDeduction: _bool
    section: Optional['models.CoaSection'] = None
    journalLines: Optional[List['models.JournalLine']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.NominalAccountKeys']] = None,
        exclude: Optional[Iterable['types.NominalAccountKeys']] = None,
        required: Optional[Iterable['types.NominalAccountKeys']] = None,
        optional: Optional[Iterable['types.NominalAccountKeys']] = None,
        relations: Optional[Mapping['types.NominalAccountRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.NominalAccountKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _NominalAccount_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _NominalAccount_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _NominalAccount_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _NominalAccount_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _NominalAccount_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _NominalAccount_relational_fields:
                        raise errors.UnknownRelationalFieldError('NominalAccount', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid NominalAccount / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'NominalAccount',
            }
        )
        _created_partial_types.add(name)


class BankAccount(bases.BaseBankAccount):
    """Represents a BankAccount record"""

    id: _str
    companyId: _str
    name: _str
    nominalCode: Optional[_str] = None
    currency: _str
    isActive: _bool
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.BankAccountKeys']] = None,
        exclude: Optional[Iterable['types.BankAccountKeys']] = None,
        required: Optional[Iterable['types.BankAccountKeys']] = None,
        optional: Optional[Iterable['types.BankAccountKeys']] = None,
        relations: Optional[Mapping['types.BankAccountRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.BankAccountKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _BankAccount_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _BankAccount_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _BankAccount_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _BankAccount_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _BankAccount_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _BankAccount_relational_fields:
                        raise errors.UnknownRelationalFieldError('BankAccount', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid BankAccount / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'BankAccount',
            }
        )
        _created_partial_types.add(name)


class Customer(bases.BaseCustomer):
    """Represents a Customer record"""

    id: _str
    companyId: _str
    code: _str
    name: _str
    email: Optional[_str] = None
    phone: Optional[_str] = None
    customFields: 'fields.Json'
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CustomerKeys']] = None,
        exclude: Optional[Iterable['types.CustomerKeys']] = None,
        required: Optional[Iterable['types.CustomerKeys']] = None,
        optional: Optional[Iterable['types.CustomerKeys']] = None,
        relations: Optional[Mapping['types.CustomerRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CustomerKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Customer_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Customer_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Customer_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Customer_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Customer_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Customer_relational_fields:
                        raise errors.UnknownRelationalFieldError('Customer', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Customer / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Customer',
            }
        )
        _created_partial_types.add(name)


class Supplier(bases.BaseSupplier):
    """Represents a Supplier record"""

    id: _str
    companyId: _str
    code: _str
    name: _str
    email: Optional[_str] = None
    phone: Optional[_str] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SupplierKeys']] = None,
        exclude: Optional[Iterable['types.SupplierKeys']] = None,
        required: Optional[Iterable['types.SupplierKeys']] = None,
        optional: Optional[Iterable['types.SupplierKeys']] = None,
        relations: Optional[Mapping['types.SupplierRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SupplierKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Supplier_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Supplier_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Supplier_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Supplier_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Supplier_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Supplier_relational_fields:
                        raise errors.UnknownRelationalFieldError('Supplier', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Supplier / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Supplier',
            }
        )
        _created_partial_types.add(name)


class Product(bases.BaseProduct):
    """Represents a Product record"""

    id: _str
    companyId: _str
    code: _str
    name: _str
    category: Optional[_str] = None
    unit: _str
    cost: decimal.Decimal
    salePrice: decimal.Decimal
    isStock: _bool
    isArchived: _bool
    customFields: 'fields.Json'
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    uoms: Optional[List['models.ProductUom']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductKeys']] = None,
        exclude: Optional[Iterable['types.ProductKeys']] = None,
        required: Optional[Iterable['types.ProductKeys']] = None,
        optional: Optional[Iterable['types.ProductKeys']] = None,
        relations: Optional[Mapping['types.ProductRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Product_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Product_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Product_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Product_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Product_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Product_relational_fields:
                        raise errors.UnknownRelationalFieldError('Product', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Product / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Product',
            }
        )
        _created_partial_types.add(name)


class CompanyModuleEntitlement(bases.BaseCompanyModuleEntitlement):
    """Subscription / license gate per module â€” P11.
    """

    id: _str
    companyId: _str
    moduleCode: _str
    enabled: _bool
    updatedAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CompanyModuleEntitlementKeys']] = None,
        exclude: Optional[Iterable['types.CompanyModuleEntitlementKeys']] = None,
        required: Optional[Iterable['types.CompanyModuleEntitlementKeys']] = None,
        optional: Optional[Iterable['types.CompanyModuleEntitlementKeys']] = None,
        relations: Optional[Mapping['types.CompanyModuleEntitlementRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CompanyModuleEntitlementKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _CompanyModuleEntitlement_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _CompanyModuleEntitlement_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _CompanyModuleEntitlement_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _CompanyModuleEntitlement_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _CompanyModuleEntitlement_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _CompanyModuleEntitlement_relational_fields:
                        raise errors.UnknownRelationalFieldError('CompanyModuleEntitlement', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid CompanyModuleEntitlement / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'CompanyModuleEntitlement',
            }
        )
        _created_partial_types.add(name)


class CustomFieldDefinition(bases.BaseCustomFieldDefinition):
    """Configurable custom fields for customers and products â€” P11.
    """

    id: _str
    companyId: _str
    entityType: _str
    fieldKey: _str
    label: _str
    fieldType: _str
    isRequired: _bool
    picklistOptions: Optional['fields.Json'] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CustomFieldDefinitionKeys']] = None,
        exclude: Optional[Iterable['types.CustomFieldDefinitionKeys']] = None,
        required: Optional[Iterable['types.CustomFieldDefinitionKeys']] = None,
        optional: Optional[Iterable['types.CustomFieldDefinitionKeys']] = None,
        relations: Optional[Mapping['types.CustomFieldDefinitionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CustomFieldDefinitionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _CustomFieldDefinition_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _CustomFieldDefinition_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _CustomFieldDefinition_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _CustomFieldDefinition_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _CustomFieldDefinition_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _CustomFieldDefinition_relational_fields:
                        raise errors.UnknownRelationalFieldError('CustomFieldDefinition', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid CustomFieldDefinition / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'CustomFieldDefinition',
            }
        )
        _created_partial_types.add(name)


class SubscriptionRecord(bases.BaseSubscriptionRecord):
    """External billing provider mirror (Stripe/etc.) â€” P12 stub.
    """

    id: _str
    companyId: _str
    planCode: _str
    externalCustomerId: Optional[_str] = None
    status: _str
    currentPeriodEnd: Optional[datetime.datetime] = None
    metadata: 'fields.Json'
    updatedAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SubscriptionRecordKeys']] = None,
        exclude: Optional[Iterable['types.SubscriptionRecordKeys']] = None,
        required: Optional[Iterable['types.SubscriptionRecordKeys']] = None,
        optional: Optional[Iterable['types.SubscriptionRecordKeys']] = None,
        relations: Optional[Mapping['types.SubscriptionRecordRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SubscriptionRecordKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SubscriptionRecord_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SubscriptionRecord_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SubscriptionRecord_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SubscriptionRecord_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SubscriptionRecord_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SubscriptionRecord_relational_fields:
                        raise errors.UnknownRelationalFieldError('SubscriptionRecord', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SubscriptionRecord / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SubscriptionRecord',
            }
        )
        _created_partial_types.add(name)


class ProductUom(bases.BaseProductUom):
    """Multi-UOM price tiers per product â€” P11.
    """

    id: _str
    companyId: _str
    productId: _str
    unitCode: _str
    conversionFactor: decimal.Decimal
    salePrice: decimal.Decimal
    isDefault: _bool
    product: Optional['models.Product'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductUomKeys']] = None,
        exclude: Optional[Iterable['types.ProductUomKeys']] = None,
        required: Optional[Iterable['types.ProductUomKeys']] = None,
        optional: Optional[Iterable['types.ProductUomKeys']] = None,
        relations: Optional[Mapping['types.ProductUomRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductUomKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProductUom_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProductUom_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProductUom_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProductUom_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProductUom_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProductUom_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProductUom', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProductUom / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProductUom',
            }
        )
        _created_partial_types.add(name)


class Project(bases.BaseProject):
    """Represents a Project record"""

    id: _str
    companyId: _str
    code: _str
    name: _str
    isActive: _bool
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProjectKeys']] = None,
        exclude: Optional[Iterable['types.ProjectKeys']] = None,
        required: Optional[Iterable['types.ProjectKeys']] = None,
        optional: Optional[Iterable['types.ProjectKeys']] = None,
        relations: Optional[Mapping['types.ProjectRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProjectKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Project_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Project_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Project_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Project_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Project_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Project_relational_fields:
                        raise errors.UnknownRelationalFieldError('Project', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Project / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Project',
            }
        )
        _created_partial_types.add(name)


class Location(bases.BaseLocation):
    """Represents a Location record"""

    id: _str
    companyId: _str
    code: _str
    name: _str
    isMain: _bool
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.LocationKeys']] = None,
        exclude: Optional[Iterable['types.LocationKeys']] = None,
        required: Optional[Iterable['types.LocationKeys']] = None,
        optional: Optional[Iterable['types.LocationKeys']] = None,
        relations: Optional[Mapping['types.LocationRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.LocationKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Location_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Location_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Location_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Location_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Location_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Location_relational_fields:
                        raise errors.UnknownRelationalFieldError('Location', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Location / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Location',
            }
        )
        _created_partial_types.add(name)


class Journal(bases.BaseJournal):
    """Represents a Journal record"""

    id: _str
    companyId: _str
    journalNumber: _str
    journalDate: datetime.datetime
    refNo: Optional[_str] = None
    totalAmount: decimal.Decimal
    status: _str
    sourceType: Optional[_str] = None
    """e.g. SALES_INVOICE, STOCK_ADJUSTMENT â€” P0 traceability
    """

    sourceId: Optional[_str] = None
    correlationId: Optional[_str] = None
    reversesJournalId: Optional[_str] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    lines: Optional[List['models.JournalLine']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.JournalKeys']] = None,
        exclude: Optional[Iterable['types.JournalKeys']] = None,
        required: Optional[Iterable['types.JournalKeys']] = None,
        optional: Optional[Iterable['types.JournalKeys']] = None,
        relations: Optional[Mapping['types.JournalRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.JournalKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Journal_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Journal_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Journal_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Journal_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Journal_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Journal_relational_fields:
                        raise errors.UnknownRelationalFieldError('Journal', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Journal / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Journal',
            }
        )
        _created_partial_types.add(name)


class JournalLine(bases.BaseJournalLine):
    """Represents a JournalLine record"""

    id: _str
    journalId: _str
    nominalCode: _str
    nominalAccountId: Optional[_str] = None
    """Optional FK to chart nominal (P3); ``nominalCode`` remains for legacy queries.
    """

    debit: decimal.Decimal
    credit: decimal.Decimal
    projectCode: Optional[_str] = None
    journal: Optional['models.Journal'] = None
    nominalAccount: Optional['models.NominalAccount'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.JournalLineKeys']] = None,
        exclude: Optional[Iterable['types.JournalLineKeys']] = None,
        required: Optional[Iterable['types.JournalLineKeys']] = None,
        optional: Optional[Iterable['types.JournalLineKeys']] = None,
        relations: Optional[Mapping['types.JournalLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.JournalLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _JournalLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _JournalLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _JournalLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _JournalLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _JournalLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _JournalLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('JournalLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid JournalLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'JournalLine',
            }
        )
        _created_partial_types.add(name)


class SalesInvoice(bases.BaseSalesInvoice):
    """Represents a SalesInvoice record"""

    id: _str
    companyId: _str
    invoiceNumber: _str
    invoiceDate: datetime.datetime
    customerId: _str
    customerCode: Optional[_str] = None
    """Write-time snapshot (Step 3) â€” avoids JOIN to customers on reads.
    """

    customerName: Optional[_str] = None
    status: _str
    totalAmount: decimal.Decimal
    remainingAmount: decimal.Decimal
    """Open balance after receipt allocations (Phase 3 denorm).
    """

    journalId: Optional[_str] = None
    """GL journal that posted this invoice (DR AR / CR Sales). Null when defaults missing at create time.
    """

    customFields: 'fields.Json'
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    lines: Optional[List['models.SalesInvoiceLine']] = None
    allocations: Optional[List['models.SalesReceiptAllocation']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SalesInvoiceKeys']] = None,
        exclude: Optional[Iterable['types.SalesInvoiceKeys']] = None,
        required: Optional[Iterable['types.SalesInvoiceKeys']] = None,
        optional: Optional[Iterable['types.SalesInvoiceKeys']] = None,
        relations: Optional[Mapping['types.SalesInvoiceRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SalesInvoiceKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SalesInvoice_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SalesInvoice_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SalesInvoice_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SalesInvoice_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SalesInvoice_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SalesInvoice_relational_fields:
                        raise errors.UnknownRelationalFieldError('SalesInvoice', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SalesInvoice / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SalesInvoice',
            }
        )
        _created_partial_types.add(name)


class SalesInvoiceLine(bases.BaseSalesInvoiceLine):
    """Represents a SalesInvoiceLine record"""

    id: _str
    salesInvoiceId: _str
    productCode: Optional[_str] = None
    quantity: decimal.Decimal
    rate: decimal.Decimal
    lineSubtotal: decimal.Decimal
    """Net before tax (qty Ã— rate).
    """

    gstCode: Optional[_str] = None
    gstRate: decimal.Decimal
    taxAmount: decimal.Decimal
    lineTotal: decimal.Decimal
    projectCode: Optional[_str] = None
    description: Optional[_str] = None
    descriptionFields: 'fields.Json'
    batchNumber: Optional[_str] = None
    expiryDate: Optional[datetime.datetime] = None
    salesInvoice: Optional['models.SalesInvoice'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SalesInvoiceLineKeys']] = None,
        exclude: Optional[Iterable['types.SalesInvoiceLineKeys']] = None,
        required: Optional[Iterable['types.SalesInvoiceLineKeys']] = None,
        optional: Optional[Iterable['types.SalesInvoiceLineKeys']] = None,
        relations: Optional[Mapping['types.SalesInvoiceLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SalesInvoiceLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SalesInvoiceLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SalesInvoiceLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SalesInvoiceLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SalesInvoiceLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SalesInvoiceLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SalesInvoiceLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('SalesInvoiceLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SalesInvoiceLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SalesInvoiceLine',
            }
        )
        _created_partial_types.add(name)


class SupplierBill(bases.BaseSupplierBill):
    """Represents a SupplierBill record"""

    id: _str
    companyId: _str
    billNumber: _str
    billDate: datetime.datetime
    supplierId: _str
    supplierCode: Optional[_str] = None
    """Write-time snapshot (Step 3) â€” avoids JOIN to suppliers on reads.
    """

    supplierName: Optional[_str] = None
    status: _str
    totalAmount: decimal.Decimal
    remainingAmount: decimal.Decimal
    journalId: Optional[_str] = None
    """GL journal that posted this bill (DR Expense / CR AP). Null when defaults missing at create time.
    """

    customFields: 'fields.Json'
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    lines: Optional[List['models.SupplierBillLine']] = None
    allocations: Optional[List['models.SupplierPaymentAllocation']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SupplierBillKeys']] = None,
        exclude: Optional[Iterable['types.SupplierBillKeys']] = None,
        required: Optional[Iterable['types.SupplierBillKeys']] = None,
        optional: Optional[Iterable['types.SupplierBillKeys']] = None,
        relations: Optional[Mapping['types.SupplierBillRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SupplierBillKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SupplierBill_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SupplierBill_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SupplierBill_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SupplierBill_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SupplierBill_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SupplierBill_relational_fields:
                        raise errors.UnknownRelationalFieldError('SupplierBill', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SupplierBill / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SupplierBill',
            }
        )
        _created_partial_types.add(name)


class SupplierBillLine(bases.BaseSupplierBillLine):
    """Represents a SupplierBillLine record"""

    id: _str
    supplierBillId: _str
    productCode: Optional[_str] = None
    quantity: decimal.Decimal
    rate: decimal.Decimal
    lineSubtotal: decimal.Decimal
    gstCode: Optional[_str] = None
    gstRate: decimal.Decimal
    taxAmount: decimal.Decimal
    lineTotal: decimal.Decimal
    description: Optional[_str] = None
    descriptionFields: 'fields.Json'
    batchNumber: Optional[_str] = None
    expiryDate: Optional[datetime.datetime] = None
    supplierBill: Optional['models.SupplierBill'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SupplierBillLineKeys']] = None,
        exclude: Optional[Iterable['types.SupplierBillLineKeys']] = None,
        required: Optional[Iterable['types.SupplierBillLineKeys']] = None,
        optional: Optional[Iterable['types.SupplierBillLineKeys']] = None,
        relations: Optional[Mapping['types.SupplierBillLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SupplierBillLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SupplierBillLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SupplierBillLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SupplierBillLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SupplierBillLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SupplierBillLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SupplierBillLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('SupplierBillLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SupplierBillLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SupplierBillLine',
            }
        )
        _created_partial_types.add(name)


class BankPayment(bases.BaseBankPayment):
    """Represents a BankPayment record"""

    id: _str
    companyId: _str
    voucherNumber: _str
    paymentDate: datetime.datetime
    bankAccountId: _str
    nominalCode: Optional[_str] = None
    """Counterpart nominal for the single-line journal (DR this code, CR bank account's nominal).
    When null, the payment is recorded but no journal posts.
    """

    totalAmount: decimal.Decimal
    status: _str
    journalId: Optional[_str] = None
    """GL journal that posted this payment.
    """

    reconciledAt: Optional[datetime.datetime] = None
    customFields: 'fields.Json'
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    customerAdvanceReturn: Optional['models.CustomerAdvanceReturn'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.BankPaymentKeys']] = None,
        exclude: Optional[Iterable['types.BankPaymentKeys']] = None,
        required: Optional[Iterable['types.BankPaymentKeys']] = None,
        optional: Optional[Iterable['types.BankPaymentKeys']] = None,
        relations: Optional[Mapping['types.BankPaymentRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.BankPaymentKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _BankPayment_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _BankPayment_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _BankPayment_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _BankPayment_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _BankPayment_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _BankPayment_relational_fields:
                        raise errors.UnknownRelationalFieldError('BankPayment', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid BankPayment / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'BankPayment',
            }
        )
        _created_partial_types.add(name)


class SalesReceipt(bases.BaseSalesReceipt):
    """Customer receipt (SR). Money in, cleared against AR via allocations.
    """

    id: _str
    companyId: _str
    receiptNumber: _str
    receiptDate: datetime.datetime
    customerId: _str
    customerCode: Optional[_str] = None
    customerName: Optional[_str] = None
    bankAccountId: _str
    totalAmount: decimal.Decimal
    status: _str
    journalId: Optional[_str] = None
    """GL journal: DR bank's nominal, CR receivables nominal.
    """

    reconciledAt: Optional[datetime.datetime] = None
    customFields: 'fields.Json'
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    allocations: Optional[List['models.SalesReceiptAllocation']] = None
    advanceReturns: Optional[List['models.CustomerAdvanceReturn']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SalesReceiptKeys']] = None,
        exclude: Optional[Iterable['types.SalesReceiptKeys']] = None,
        required: Optional[Iterable['types.SalesReceiptKeys']] = None,
        optional: Optional[Iterable['types.SalesReceiptKeys']] = None,
        relations: Optional[Mapping['types.SalesReceiptRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SalesReceiptKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SalesReceipt_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SalesReceipt_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SalesReceipt_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SalesReceipt_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SalesReceipt_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SalesReceipt_relational_fields:
                        raise errors.UnknownRelationalFieldError('SalesReceipt', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SalesReceipt / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SalesReceipt',
            }
        )
        _created_partial_types.add(name)


class SupplierPayment(bases.BaseSupplierPayment):
    """Supplier payment (VP). Money out, cleared against AP via allocations.
    """

    id: _str
    companyId: _str
    voucherNumber: _str
    paymentDate: datetime.datetime
    supplierId: _str
    supplierCode: Optional[_str] = None
    supplierName: Optional[_str] = None
    bankAccountId: _str
    totalAmount: decimal.Decimal
    status: _str
    journalId: Optional[_str] = None
    """GL journal: DR payables nominal, CR bank's nominal.
    """

    reconciledAt: Optional[datetime.datetime] = None
    customFields: 'fields.Json'
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    allocations: Optional[List['models.SupplierPaymentAllocation']] = None
    advanceReturns: Optional[List['models.SupplierAdvanceReturn']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SupplierPaymentKeys']] = None,
        exclude: Optional[Iterable['types.SupplierPaymentKeys']] = None,
        required: Optional[Iterable['types.SupplierPaymentKeys']] = None,
        optional: Optional[Iterable['types.SupplierPaymentKeys']] = None,
        relations: Optional[Mapping['types.SupplierPaymentRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SupplierPaymentKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SupplierPayment_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SupplierPayment_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SupplierPayment_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SupplierPayment_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SupplierPayment_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SupplierPayment_relational_fields:
                        raise errors.UnknownRelationalFieldError('SupplierPayment', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SupplierPayment / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SupplierPayment',
            }
        )
        _created_partial_types.add(name)


class SalesReceiptAllocation(bases.BaseSalesReceiptAllocation):
    """Allocation of a customer receipt against a specific sales invoice (FIFO or explicit).
    """

    id: _str
    salesReceiptId: _str
    salesInvoiceId: _str
    amount: decimal.Decimal
    salesReceipt: Optional['models.SalesReceipt'] = None
    salesInvoice: Optional['models.SalesInvoice'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SalesReceiptAllocationKeys']] = None,
        exclude: Optional[Iterable['types.SalesReceiptAllocationKeys']] = None,
        required: Optional[Iterable['types.SalesReceiptAllocationKeys']] = None,
        optional: Optional[Iterable['types.SalesReceiptAllocationKeys']] = None,
        relations: Optional[Mapping['types.SalesReceiptAllocationRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SalesReceiptAllocationKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SalesReceiptAllocation_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SalesReceiptAllocation_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SalesReceiptAllocation_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SalesReceiptAllocation_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SalesReceiptAllocation_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SalesReceiptAllocation_relational_fields:
                        raise errors.UnknownRelationalFieldError('SalesReceiptAllocation', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SalesReceiptAllocation / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SalesReceiptAllocation',
            }
        )
        _created_partial_types.add(name)


class SupplierPaymentAllocation(bases.BaseSupplierPaymentAllocation):
    """Allocation of a supplier payment against a specific supplier bill.
    """

    id: _str
    supplierPaymentId: _str
    supplierBillId: _str
    amount: decimal.Decimal
    supplierPayment: Optional['models.SupplierPayment'] = None
    supplierBill: Optional['models.SupplierBill'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SupplierPaymentAllocationKeys']] = None,
        exclude: Optional[Iterable['types.SupplierPaymentAllocationKeys']] = None,
        required: Optional[Iterable['types.SupplierPaymentAllocationKeys']] = None,
        optional: Optional[Iterable['types.SupplierPaymentAllocationKeys']] = None,
        relations: Optional[Mapping['types.SupplierPaymentAllocationRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SupplierPaymentAllocationKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SupplierPaymentAllocation_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SupplierPaymentAllocation_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SupplierPaymentAllocation_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SupplierPaymentAllocation_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SupplierPaymentAllocation_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SupplierPaymentAllocation_relational_fields:
                        raise errors.UnknownRelationalFieldError('SupplierPaymentAllocation', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SupplierPaymentAllocation / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SupplierPaymentAllocation',
            }
        )
        _created_partial_types.add(name)


class CustomerAdvanceReturn(bases.BaseCustomerAdvanceReturn):
    """Customer advance returned to payer â€” FA Â§5.8 (bank payment out, DR AR / CR bank).
    """

    id: _str
    companyId: _str
    salesReceiptId: _str
    bankPaymentId: _str
    amount: decimal.Decimal
    returnDate: datetime.datetime
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    salesReceipt: Optional['models.SalesReceipt'] = None
    bankPayment: Optional['models.BankPayment'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CustomerAdvanceReturnKeys']] = None,
        exclude: Optional[Iterable['types.CustomerAdvanceReturnKeys']] = None,
        required: Optional[Iterable['types.CustomerAdvanceReturnKeys']] = None,
        optional: Optional[Iterable['types.CustomerAdvanceReturnKeys']] = None,
        relations: Optional[Mapping['types.CustomerAdvanceReturnRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CustomerAdvanceReturnKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _CustomerAdvanceReturn_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _CustomerAdvanceReturn_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _CustomerAdvanceReturn_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _CustomerAdvanceReturn_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _CustomerAdvanceReturn_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _CustomerAdvanceReturn_relational_fields:
                        raise errors.UnknownRelationalFieldError('CustomerAdvanceReturn', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid CustomerAdvanceReturn / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'CustomerAdvanceReturn',
            }
        )
        _created_partial_types.add(name)


class SupplierAdvanceReturn(bases.BaseSupplierAdvanceReturn):
    """Supplier advance returned to company â€” FA Â§6.4 (bank receipt in, DR bank / CR AP).
    """

    id: _str
    companyId: _str
    supplierPaymentId: _str
    bankReceiptId: _str
    amount: decimal.Decimal
    returnDate: datetime.datetime
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    supplierPayment: Optional['models.SupplierPayment'] = None
    bankReceipt: Optional['models.BankReceipt'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SupplierAdvanceReturnKeys']] = None,
        exclude: Optional[Iterable['types.SupplierAdvanceReturnKeys']] = None,
        required: Optional[Iterable['types.SupplierAdvanceReturnKeys']] = None,
        optional: Optional[Iterable['types.SupplierAdvanceReturnKeys']] = None,
        relations: Optional[Mapping['types.SupplierAdvanceReturnRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SupplierAdvanceReturnKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SupplierAdvanceReturn_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SupplierAdvanceReturn_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SupplierAdvanceReturn_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SupplierAdvanceReturn_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SupplierAdvanceReturn_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SupplierAdvanceReturn_relational_fields:
                        raise errors.UnknownRelationalFieldError('SupplierAdvanceReturn', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SupplierAdvanceReturn / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SupplierAdvanceReturn',
            }
        )
        _created_partial_types.add(name)


class BankReceipt(bases.BaseBankReceipt):
    """Bank receipt (IR). Money in from non-AR sources (refunds, capital, etc.).
    """

    id: _str
    companyId: _str
    voucherNumber: _str
    receiptDate: datetime.datetime
    bankAccountId: _str
    nominalCode: Optional[_str] = None
    """Counterpart nominal (CR side) â€” typically an income or refund-control code.
    """

    totalAmount: decimal.Decimal
    status: _str
    journalId: Optional[_str] = None
    """GL journal: DR bank's nominal, CR counterpart.
    """

    reconciledAt: Optional[datetime.datetime] = None
    customFields: 'fields.Json'
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    supplierAdvanceReturn: Optional['models.SupplierAdvanceReturn'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.BankReceiptKeys']] = None,
        exclude: Optional[Iterable['types.BankReceiptKeys']] = None,
        required: Optional[Iterable['types.BankReceiptKeys']] = None,
        optional: Optional[Iterable['types.BankReceiptKeys']] = None,
        relations: Optional[Mapping['types.BankReceiptRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.BankReceiptKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _BankReceipt_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _BankReceipt_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _BankReceipt_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _BankReceipt_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _BankReceipt_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _BankReceipt_relational_fields:
                        raise errors.UnknownRelationalFieldError('BankReceipt', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid BankReceipt / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'BankReceipt',
            }
        )
        _created_partial_types.add(name)


class Quotation(bases.BaseQuotation):
    """Quotation (SQ) â€” pre-sale offer. No GL impact.
    """

    id: _str
    companyId: _str
    quotationNumber: _str
    quotationDate: datetime.datetime
    customerId: _str
    customerCode: Optional[_str] = None
    customerName: Optional[_str] = None
    status: _str
    """draft | approved | rejected | accepted | converted
    """

    totalAmount: decimal.Decimal
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    lines: Optional[List['models.QuotationLine']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.QuotationKeys']] = None,
        exclude: Optional[Iterable['types.QuotationKeys']] = None,
        required: Optional[Iterable['types.QuotationKeys']] = None,
        optional: Optional[Iterable['types.QuotationKeys']] = None,
        relations: Optional[Mapping['types.QuotationRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.QuotationKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Quotation_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Quotation_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Quotation_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Quotation_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Quotation_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Quotation_relational_fields:
                        raise errors.UnknownRelationalFieldError('Quotation', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Quotation / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Quotation',
            }
        )
        _created_partial_types.add(name)


class QuotationLine(bases.BaseQuotationLine):
    """Represents a QuotationLine record"""

    id: _str
    quotationId: _str
    productCode: Optional[_str] = None
    quantity: decimal.Decimal
    rate: decimal.Decimal
    lineSubtotal: decimal.Decimal
    gstCode: Optional[_str] = None
    gstRate: decimal.Decimal
    taxAmount: decimal.Decimal
    lineTotal: decimal.Decimal
    quotation: Optional['models.Quotation'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.QuotationLineKeys']] = None,
        exclude: Optional[Iterable['types.QuotationLineKeys']] = None,
        required: Optional[Iterable['types.QuotationLineKeys']] = None,
        optional: Optional[Iterable['types.QuotationLineKeys']] = None,
        relations: Optional[Mapping['types.QuotationLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.QuotationLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _QuotationLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _QuotationLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _QuotationLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _QuotationLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _QuotationLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _QuotationLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('QuotationLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid QuotationLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'QuotationLine',
            }
        )
        _created_partial_types.add(name)


class SalesOrder(bases.BaseSalesOrder):
    """Sales order (SO). No GL impact until invoiced.
    """

    id: _str
    companyId: _str
    orderNumber: _str
    orderDate: datetime.datetime
    customerId: _str
    customerCode: Optional[_str] = None
    customerName: Optional[_str] = None
    status: _str
    """in_progress | approved | rejected | invoiced
    """

    totalAmount: decimal.Decimal
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    lines: Optional[List['models.SalesOrderLine']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SalesOrderKeys']] = None,
        exclude: Optional[Iterable['types.SalesOrderKeys']] = None,
        required: Optional[Iterable['types.SalesOrderKeys']] = None,
        optional: Optional[Iterable['types.SalesOrderKeys']] = None,
        relations: Optional[Mapping['types.SalesOrderRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SalesOrderKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SalesOrder_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SalesOrder_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SalesOrder_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SalesOrder_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SalesOrder_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SalesOrder_relational_fields:
                        raise errors.UnknownRelationalFieldError('SalesOrder', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SalesOrder / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SalesOrder',
            }
        )
        _created_partial_types.add(name)


class SalesOrderLine(bases.BaseSalesOrderLine):
    """Represents a SalesOrderLine record"""

    id: _str
    salesOrderId: _str
    productCode: Optional[_str] = None
    quantity: decimal.Decimal
    rate: decimal.Decimal
    lineSubtotal: decimal.Decimal
    gstCode: Optional[_str] = None
    gstRate: decimal.Decimal
    taxAmount: decimal.Decimal
    lineTotal: decimal.Decimal
    projectCode: Optional[_str] = None
    salesOrder: Optional['models.SalesOrder'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SalesOrderLineKeys']] = None,
        exclude: Optional[Iterable['types.SalesOrderLineKeys']] = None,
        required: Optional[Iterable['types.SalesOrderLineKeys']] = None,
        optional: Optional[Iterable['types.SalesOrderLineKeys']] = None,
        relations: Optional[Mapping['types.SalesOrderLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SalesOrderLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SalesOrderLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SalesOrderLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SalesOrderLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SalesOrderLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SalesOrderLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SalesOrderLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('SalesOrderLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SalesOrderLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SalesOrderLine',
            }
        )
        _created_partial_types.add(name)


class PurchaseOrder(bases.BasePurchaseOrder):
    """Purchase order (PO). No GL impact until billed.
    """

    id: _str
    companyId: _str
    orderNumber: _str
    orderDate: datetime.datetime
    supplierId: _str
    supplierCode: Optional[_str] = None
    supplierName: Optional[_str] = None
    status: _str
    """in_progress | approved | rejected | billed
    """

    totalAmount: decimal.Decimal
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    lines: Optional[List['models.PurchaseOrderLine']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PurchaseOrderKeys']] = None,
        exclude: Optional[Iterable['types.PurchaseOrderKeys']] = None,
        required: Optional[Iterable['types.PurchaseOrderKeys']] = None,
        optional: Optional[Iterable['types.PurchaseOrderKeys']] = None,
        relations: Optional[Mapping['types.PurchaseOrderRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PurchaseOrderKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _PurchaseOrder_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _PurchaseOrder_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _PurchaseOrder_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _PurchaseOrder_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _PurchaseOrder_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _PurchaseOrder_relational_fields:
                        raise errors.UnknownRelationalFieldError('PurchaseOrder', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid PurchaseOrder / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'PurchaseOrder',
            }
        )
        _created_partial_types.add(name)


class PurchaseOrderLine(bases.BasePurchaseOrderLine):
    """Represents a PurchaseOrderLine record"""

    id: _str
    purchaseOrderId: _str
    productCode: Optional[_str] = None
    quantity: decimal.Decimal
    rate: decimal.Decimal
    lineSubtotal: decimal.Decimal
    gstCode: Optional[_str] = None
    gstRate: decimal.Decimal
    taxAmount: decimal.Decimal
    lineTotal: decimal.Decimal
    purchaseOrder: Optional['models.PurchaseOrder'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PurchaseOrderLineKeys']] = None,
        exclude: Optional[Iterable['types.PurchaseOrderLineKeys']] = None,
        required: Optional[Iterable['types.PurchaseOrderLineKeys']] = None,
        optional: Optional[Iterable['types.PurchaseOrderLineKeys']] = None,
        relations: Optional[Mapping['types.PurchaseOrderLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PurchaseOrderLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _PurchaseOrderLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _PurchaseOrderLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _PurchaseOrderLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _PurchaseOrderLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _PurchaseOrderLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _PurchaseOrderLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('PurchaseOrderLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid PurchaseOrderLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'PurchaseOrderLine',
            }
        )
        _created_partial_types.add(name)


class SalesCredit(bases.BaseSalesCredit):
    """Sales credit (SC) â€” reverses an invoice. Posts DR sales / CR receivables.
    """

    id: _str
    companyId: _str
    creditNumber: _str
    creditDate: datetime.datetime
    customerId: _str
    customerCode: Optional[_str] = None
    customerName: Optional[_str] = None
    status: _str
    totalAmount: decimal.Decimal
    journalId: Optional[_str] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    lines: Optional[List['models.SalesCreditLine']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SalesCreditKeys']] = None,
        exclude: Optional[Iterable['types.SalesCreditKeys']] = None,
        required: Optional[Iterable['types.SalesCreditKeys']] = None,
        optional: Optional[Iterable['types.SalesCreditKeys']] = None,
        relations: Optional[Mapping['types.SalesCreditRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SalesCreditKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SalesCredit_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SalesCredit_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SalesCredit_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SalesCredit_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SalesCredit_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SalesCredit_relational_fields:
                        raise errors.UnknownRelationalFieldError('SalesCredit', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SalesCredit / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SalesCredit',
            }
        )
        _created_partial_types.add(name)


class SalesCreditLine(bases.BaseSalesCreditLine):
    """Represents a SalesCreditLine record"""

    id: _str
    salesCreditId: _str
    productCode: Optional[_str] = None
    quantity: decimal.Decimal
    rate: decimal.Decimal
    lineSubtotal: decimal.Decimal
    gstCode: Optional[_str] = None
    gstRate: decimal.Decimal
    taxAmount: decimal.Decimal
    lineTotal: decimal.Decimal
    salesCredit: Optional['models.SalesCredit'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SalesCreditLineKeys']] = None,
        exclude: Optional[Iterable['types.SalesCreditLineKeys']] = None,
        required: Optional[Iterable['types.SalesCreditLineKeys']] = None,
        optional: Optional[Iterable['types.SalesCreditLineKeys']] = None,
        relations: Optional[Mapping['types.SalesCreditLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SalesCreditLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SalesCreditLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SalesCreditLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SalesCreditLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SalesCreditLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SalesCreditLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SalesCreditLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('SalesCreditLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SalesCreditLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SalesCreditLine',
            }
        )
        _created_partial_types.add(name)


class SupplierCredit(bases.BaseSupplierCredit):
    """Supplier credit (VC) â€” reverses a bill. Posts DR payables / CR purchases.
    """

    id: _str
    companyId: _str
    creditNumber: _str
    creditDate: datetime.datetime
    supplierId: _str
    supplierCode: Optional[_str] = None
    supplierName: Optional[_str] = None
    status: _str
    totalAmount: decimal.Decimal
    journalId: Optional[_str] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    lines: Optional[List['models.SupplierCreditLine']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SupplierCreditKeys']] = None,
        exclude: Optional[Iterable['types.SupplierCreditKeys']] = None,
        required: Optional[Iterable['types.SupplierCreditKeys']] = None,
        optional: Optional[Iterable['types.SupplierCreditKeys']] = None,
        relations: Optional[Mapping['types.SupplierCreditRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SupplierCreditKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SupplierCredit_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SupplierCredit_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SupplierCredit_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SupplierCredit_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SupplierCredit_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SupplierCredit_relational_fields:
                        raise errors.UnknownRelationalFieldError('SupplierCredit', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SupplierCredit / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SupplierCredit',
            }
        )
        _created_partial_types.add(name)


class SupplierCreditLine(bases.BaseSupplierCreditLine):
    """Represents a SupplierCreditLine record"""

    id: _str
    supplierCreditId: _str
    productCode: Optional[_str] = None
    quantity: decimal.Decimal
    rate: decimal.Decimal
    lineSubtotal: decimal.Decimal
    gstCode: Optional[_str] = None
    gstRate: decimal.Decimal
    taxAmount: decimal.Decimal
    lineTotal: decimal.Decimal
    supplierCredit: Optional['models.SupplierCredit'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SupplierCreditLineKeys']] = None,
        exclude: Optional[Iterable['types.SupplierCreditLineKeys']] = None,
        required: Optional[Iterable['types.SupplierCreditLineKeys']] = None,
        optional: Optional[Iterable['types.SupplierCreditLineKeys']] = None,
        relations: Optional[Mapping['types.SupplierCreditLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SupplierCreditLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SupplierCreditLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SupplierCreditLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SupplierCreditLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SupplierCreditLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SupplierCreditLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SupplierCreditLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('SupplierCreditLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SupplierCreditLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SupplierCreditLine',
            }
        )
        _created_partial_types.add(name)


class BankTransfer(bases.BaseBankTransfer):
    """Bank transfer â€” funds between two bank/cash books.
    """

    id: _str
    companyId: _str
    voucherNumber: _str
    transferDate: datetime.datetime
    fromBankAccountId: _str
    toBankAccountId: _str
    totalAmount: decimal.Decimal
    status: _str
    journalId: Optional[_str] = None
    """GL journal: DR to-bank nominal, CR from-bank nominal.
    """

    reconciledAt: Optional[datetime.datetime] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.BankTransferKeys']] = None,
        exclude: Optional[Iterable['types.BankTransferKeys']] = None,
        required: Optional[Iterable['types.BankTransferKeys']] = None,
        optional: Optional[Iterable['types.BankTransferKeys']] = None,
        relations: Optional[Mapping['types.BankTransferRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.BankTransferKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _BankTransfer_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _BankTransfer_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _BankTransfer_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _BankTransfer_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _BankTransfer_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _BankTransfer_relational_fields:
                        raise errors.UnknownRelationalFieldError('BankTransfer', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid BankTransfer / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'BankTransfer',
            }
        )
        _created_partial_types.add(name)


class Attachment(bases.BaseAttachment):
    """Represents a Attachment record"""

    id: _str
    companyId: _str
    entityType: _str
    entityId: _str
    fileName: _str
    storageKey: _str
    mimeType: Optional[_str] = None
    byteSize: _int
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.AttachmentKeys']] = None,
        exclude: Optional[Iterable['types.AttachmentKeys']] = None,
        required: Optional[Iterable['types.AttachmentKeys']] = None,
        optional: Optional[Iterable['types.AttachmentKeys']] = None,
        relations: Optional[Mapping['types.AttachmentRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.AttachmentKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Attachment_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Attachment_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Attachment_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Attachment_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Attachment_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Attachment_relational_fields:
                        raise errors.UnknownRelationalFieldError('Attachment', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Attachment / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Attachment',
            }
        )
        _created_partial_types.add(name)


class DocumentNumberSequence(bases.BaseDocumentNumberSequence):
    """Represents a DocumentNumberSequence record"""

    id: _str
    companyId: _str
    key: _str
    nextValue: _int
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DocumentNumberSequenceKeys']] = None,
        exclude: Optional[Iterable['types.DocumentNumberSequenceKeys']] = None,
        required: Optional[Iterable['types.DocumentNumberSequenceKeys']] = None,
        optional: Optional[Iterable['types.DocumentNumberSequenceKeys']] = None,
        relations: Optional[Mapping['types.DocumentNumberSequenceRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DocumentNumberSequenceKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _DocumentNumberSequence_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _DocumentNumberSequence_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _DocumentNumberSequence_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _DocumentNumberSequence_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _DocumentNumberSequence_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _DocumentNumberSequence_relational_fields:
                        raise errors.UnknownRelationalFieldError('DocumentNumberSequence', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid DocumentNumberSequence / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'DocumentNumberSequence',
            }
        )
        _created_partial_types.add(name)


class ImportJob(bases.BaseImportJob):
    """Represents a ImportJob record"""

    id: _str
    companyId: _str
    jobType: _str
    status: _str
    fileName: Optional[_str] = None
    payload: 'fields.Json'
    """Inline rows for API-driven imports (CSV parsed client-side or JSON upload).
    """

    errorCount: _int
    resultSummary: Optional[_str] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ImportJobKeys']] = None,
        exclude: Optional[Iterable['types.ImportJobKeys']] = None,
        required: Optional[Iterable['types.ImportJobKeys']] = None,
        optional: Optional[Iterable['types.ImportJobKeys']] = None,
        relations: Optional[Mapping['types.ImportJobRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ImportJobKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ImportJob_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ImportJob_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ImportJob_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ImportJob_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ImportJob_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ImportJob_relational_fields:
                        raise errors.UnknownRelationalFieldError('ImportJob', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ImportJob / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ImportJob',
            }
        )
        _created_partial_types.add(name)


class TransactionTemplate(bases.BaseTransactionTemplate):
    """Saved line-grid templates for SI/VI/bank vouchers â€” FA Â§3.3.
    """

    id: _str
    companyId: _str
    module: _str
    """e.g. sales_invoice, supplier_bill, bank_payment
    """

    name: _str
    payload: 'fields.Json'
    createdBy: Optional[_str] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.TransactionTemplateKeys']] = None,
        exclude: Optional[Iterable['types.TransactionTemplateKeys']] = None,
        required: Optional[Iterable['types.TransactionTemplateKeys']] = None,
        optional: Optional[Iterable['types.TransactionTemplateKeys']] = None,
        relations: Optional[Mapping['types.TransactionTemplateRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.TransactionTemplateKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _TransactionTemplate_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _TransactionTemplate_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _TransactionTemplate_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _TransactionTemplate_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _TransactionTemplate_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _TransactionTemplate_relational_fields:
                        raise errors.UnknownRelationalFieldError('TransactionTemplate', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid TransactionTemplate / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'TransactionTemplate',
            }
        )
        _created_partial_types.add(name)


class RecurringSchedule(bases.BaseRecurringSchedule):
    """Scheduled recurring documents â€” FA Â§3.3.
    """

    id: _str
    companyId: _str
    name: _str
    module: _str
    """sales_invoice, supplier_bill, bank_payment, journal
    """

    frequency: _str
    interval: _int
    nextRunDate: datetime.datetime
    endDate: Optional[datetime.datetime] = None
    isActive: _bool
    payload: 'fields.Json'
    lastRunAt: Optional[datetime.datetime] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.RecurringScheduleKeys']] = None,
        exclude: Optional[Iterable['types.RecurringScheduleKeys']] = None,
        required: Optional[Iterable['types.RecurringScheduleKeys']] = None,
        optional: Optional[Iterable['types.RecurringScheduleKeys']] = None,
        relations: Optional[Mapping['types.RecurringScheduleRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.RecurringScheduleKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _RecurringSchedule_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _RecurringSchedule_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _RecurringSchedule_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _RecurringSchedule_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _RecurringSchedule_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _RecurringSchedule_relational_fields:
                        raise errors.UnknownRelationalFieldError('RecurringSchedule', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid RecurringSchedule / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'RecurringSchedule',
            }
        )
        _created_partial_types.add(name)


class OutboxEvent(bases.BaseOutboxEvent):
    """Durable async work queue (imports, report runs).
    """

    id: _str
    companyId: _str
    eventType: _str
    payload: 'fields.Json'
    status: _str
    attempts: _int
    lastError: Optional[_str] = None
    lockedAt: Optional[datetime.datetime] = None
    nextAttemptAt: Optional[datetime.datetime] = None
    createdAt: datetime.datetime
    processedAt: Optional[datetime.datetime] = None
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.OutboxEventKeys']] = None,
        exclude: Optional[Iterable['types.OutboxEventKeys']] = None,
        required: Optional[Iterable['types.OutboxEventKeys']] = None,
        optional: Optional[Iterable['types.OutboxEventKeys']] = None,
        relations: Optional[Mapping['types.OutboxEventRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.OutboxEventKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _OutboxEvent_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _OutboxEvent_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _OutboxEvent_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _OutboxEvent_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _OutboxEvent_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _OutboxEvent_relational_fields:
                        raise errors.UnknownRelationalFieldError('OutboxEvent', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid OutboxEvent / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'OutboxEvent',
            }
        )
        _created_partial_types.add(name)


class ReportRun(bases.BaseReportRun):
    """Persisted report execution (replaces in-memory runs).
    """

    id: _str
    companyId: _str
    reportId: _str
    status: _str
    criteria: 'fields.Json'
    rowCount: _int
    rows: 'fields.Json'
    storageKey: Optional[_str] = None
    """Filesystem or object-store path when row payload is stored externally (Phase 2).
    """

    errorMessage: Optional[_str] = None
    createdAt: datetime.datetime
    completedAt: Optional[datetime.datetime] = None
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ReportRunKeys']] = None,
        exclude: Optional[Iterable['types.ReportRunKeys']] = None,
        required: Optional[Iterable['types.ReportRunKeys']] = None,
        optional: Optional[Iterable['types.ReportRunKeys']] = None,
        relations: Optional[Mapping['types.ReportRunRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ReportRunKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ReportRun_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ReportRun_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ReportRun_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ReportRun_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ReportRun_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ReportRun_relational_fields:
                        raise errors.UnknownRelationalFieldError('ReportRun', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ReportRun / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ReportRun',
            }
        )
        _created_partial_types.add(name)


class BankReconciliation(bases.BaseBankReconciliation):
    """Bank reconciliation session â€” catalog Â§4.5.
    """

    id: _str
    companyId: _str
    bankAccountId: _str
    statementDate: datetime.datetime
    statementBalance: decimal.Decimal
    status: _str
    notes: Optional[_str] = None
    createdAt: datetime.datetime
    completedAt: Optional[datetime.datetime] = None
    company: Optional['models.Company'] = None
    items: Optional[List['models.BankReconciliationItem']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.BankReconciliationKeys']] = None,
        exclude: Optional[Iterable['types.BankReconciliationKeys']] = None,
        required: Optional[Iterable['types.BankReconciliationKeys']] = None,
        optional: Optional[Iterable['types.BankReconciliationKeys']] = None,
        relations: Optional[Mapping['types.BankReconciliationRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.BankReconciliationKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _BankReconciliation_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _BankReconciliation_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _BankReconciliation_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _BankReconciliation_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _BankReconciliation_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _BankReconciliation_relational_fields:
                        raise errors.UnknownRelationalFieldError('BankReconciliation', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid BankReconciliation / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'BankReconciliation',
            }
        )
        _created_partial_types.add(name)


class BankReconciliationItem(bases.BaseBankReconciliationItem):
    """Represents a BankReconciliationItem record"""

    id: _str
    reconciliationId: _str
    itemType: _str
    itemId: _str
    transactionDate: datetime.datetime
    amount: decimal.Decimal
    reference: Optional[_str] = None
    isCleared: _bool
    reconciliation: Optional['models.BankReconciliation'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.BankReconciliationItemKeys']] = None,
        exclude: Optional[Iterable['types.BankReconciliationItemKeys']] = None,
        required: Optional[Iterable['types.BankReconciliationItemKeys']] = None,
        optional: Optional[Iterable['types.BankReconciliationItemKeys']] = None,
        relations: Optional[Mapping['types.BankReconciliationItemRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.BankReconciliationItemKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _BankReconciliationItem_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _BankReconciliationItem_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _BankReconciliationItem_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _BankReconciliationItem_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _BankReconciliationItem_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _BankReconciliationItem_relational_fields:
                        raise errors.UnknownRelationalFieldError('BankReconciliationItem', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid BankReconciliationItem / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'BankReconciliationItem',
            }
        )
        _created_partial_types.add(name)


class AuditLogEntry(bases.BaseAuditLogEntry):
    """Represents a AuditLogEntry record"""

    id: _str
    companyId: _str
    userId: Optional[_str] = None
    transactionType: _str
    transactionId: Optional[_str] = None
    status: Optional[_str] = None
    details: Optional[_str] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.AuditLogEntryKeys']] = None,
        exclude: Optional[Iterable['types.AuditLogEntryKeys']] = None,
        required: Optional[Iterable['types.AuditLogEntryKeys']] = None,
        optional: Optional[Iterable['types.AuditLogEntryKeys']] = None,
        relations: Optional[Mapping['types.AuditLogEntryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.AuditLogEntryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _AuditLogEntry_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _AuditLogEntry_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _AuditLogEntry_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _AuditLogEntry_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _AuditLogEntry_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _AuditLogEntry_relational_fields:
                        raise errors.UnknownRelationalFieldError('AuditLogEntry', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid AuditLogEntry / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'AuditLogEntry',
            }
        )
        _created_partial_types.add(name)


class ApprovalPolicy(bases.BaseApprovalPolicy):
    """Represents a ApprovalPolicy record"""

    id: _str
    companyId: _str
    entityType: _str
    rules: 'fields.Json'
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ApprovalPolicyKeys']] = None,
        exclude: Optional[Iterable['types.ApprovalPolicyKeys']] = None,
        required: Optional[Iterable['types.ApprovalPolicyKeys']] = None,
        optional: Optional[Iterable['types.ApprovalPolicyKeys']] = None,
        relations: Optional[Mapping['types.ApprovalPolicyRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ApprovalPolicyKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ApprovalPolicy_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ApprovalPolicy_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ApprovalPolicy_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ApprovalPolicy_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ApprovalPolicy_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ApprovalPolicy_relational_fields:
                        raise errors.UnknownRelationalFieldError('ApprovalPolicy', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ApprovalPolicy / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ApprovalPolicy',
            }
        )
        _created_partial_types.add(name)


class ApprovalRequest(bases.BaseApprovalRequest):
    """Runtime approval queue when policy thresholds are exceeded (P3).
    """

    id: _str
    companyId: _str
    entityType: _str
    entityId: _str
    amount: decimal.Decimal
    status: _str
    requestedById: Optional[_str] = None
    approvedById: Optional[_str] = None
    approvedAt: Optional[datetime.datetime] = None
    notes: Optional[_str] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ApprovalRequestKeys']] = None,
        exclude: Optional[Iterable['types.ApprovalRequestKeys']] = None,
        required: Optional[Iterable['types.ApprovalRequestKeys']] = None,
        optional: Optional[Iterable['types.ApprovalRequestKeys']] = None,
        relations: Optional[Mapping['types.ApprovalRequestRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ApprovalRequestKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ApprovalRequest_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ApprovalRequest_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ApprovalRequest_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ApprovalRequest_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ApprovalRequest_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ApprovalRequest_relational_fields:
                        raise errors.UnknownRelationalFieldError('ApprovalRequest', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ApprovalRequest / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ApprovalRequest',
            }
        )
        _created_partial_types.add(name)


class Budget(bases.BaseBudget):
    """Represents a Budget record"""

    id: _str
    companyId: _str
    code: _str
    name: _str
    fiscalYear: _int
    isActive: _bool
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    lines: Optional[List['models.BudgetLine']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.BudgetKeys']] = None,
        exclude: Optional[Iterable['types.BudgetKeys']] = None,
        required: Optional[Iterable['types.BudgetKeys']] = None,
        optional: Optional[Iterable['types.BudgetKeys']] = None,
        relations: Optional[Mapping['types.BudgetRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.BudgetKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Budget_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Budget_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Budget_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Budget_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Budget_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Budget_relational_fields:
                        raise errors.UnknownRelationalFieldError('Budget', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Budget / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Budget',
            }
        )
        _created_partial_types.add(name)


class BudgetLine(bases.BaseBudgetLine):
    """Represents a BudgetLine record"""

    id: _str
    budgetId: _str
    nominalCode: _str
    period: _str
    amount: decimal.Decimal
    budget: Optional['models.Budget'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.BudgetLineKeys']] = None,
        exclude: Optional[Iterable['types.BudgetLineKeys']] = None,
        required: Optional[Iterable['types.BudgetLineKeys']] = None,
        optional: Optional[Iterable['types.BudgetLineKeys']] = None,
        relations: Optional[Mapping['types.BudgetLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.BudgetLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _BudgetLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _BudgetLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _BudgetLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _BudgetLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _BudgetLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _BudgetLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('BudgetLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid BudgetLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'BudgetLine',
            }
        )
        _created_partial_types.add(name)


class StockAdjustment(bases.BaseStockAdjustment):
    """Stock adjustment voucher â€” catalog Â§7.3.
    """

    id: _str
    companyId: _str
    voucherNumber: _str
    adjustmentDate: datetime.datetime
    reason: _str
    """"increase" | "decrease" | "writeoff"
    """

    notes: Optional[_str] = None
    status: _str
    journalId: Optional[_str] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    lines: Optional[List['models.StockAdjustmentLine']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.StockAdjustmentKeys']] = None,
        exclude: Optional[Iterable['types.StockAdjustmentKeys']] = None,
        required: Optional[Iterable['types.StockAdjustmentKeys']] = None,
        optional: Optional[Iterable['types.StockAdjustmentKeys']] = None,
        relations: Optional[Mapping['types.StockAdjustmentRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.StockAdjustmentKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _StockAdjustment_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _StockAdjustment_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _StockAdjustment_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _StockAdjustment_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _StockAdjustment_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _StockAdjustment_relational_fields:
                        raise errors.UnknownRelationalFieldError('StockAdjustment', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid StockAdjustment / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'StockAdjustment',
            }
        )
        _created_partial_types.add(name)


class StockAdjustmentLine(bases.BaseStockAdjustmentLine):
    """Represents a StockAdjustmentLine record"""

    id: _str
    stockAdjustmentId: _str
    productCode: _str
    locationCode: Optional[_str] = None
    quantityDelta: decimal.Decimal
    """Positive = increase, negative = decrease.
    """

    unitCost: decimal.Decimal
    stockAdjustment: Optional['models.StockAdjustment'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.StockAdjustmentLineKeys']] = None,
        exclude: Optional[Iterable['types.StockAdjustmentLineKeys']] = None,
        required: Optional[Iterable['types.StockAdjustmentLineKeys']] = None,
        optional: Optional[Iterable['types.StockAdjustmentLineKeys']] = None,
        relations: Optional[Mapping['types.StockAdjustmentLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.StockAdjustmentLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _StockAdjustmentLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _StockAdjustmentLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _StockAdjustmentLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _StockAdjustmentLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _StockAdjustmentLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _StockAdjustmentLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('StockAdjustmentLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid StockAdjustmentLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'StockAdjustmentLine',
            }
        )
        _created_partial_types.add(name)


class StockTransfer(bases.BaseStockTransfer):
    """Stock transfer voucher â€” catalog Â§7.2.
    """

    id: _str
    companyId: _str
    voucherNumber: _str
    transferDate: datetime.datetime
    fromLocationCode: _str
    toLocationCode: _str
    notes: Optional[_str] = None
    status: _str
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    lines: Optional[List['models.StockTransferLine']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.StockTransferKeys']] = None,
        exclude: Optional[Iterable['types.StockTransferKeys']] = None,
        required: Optional[Iterable['types.StockTransferKeys']] = None,
        optional: Optional[Iterable['types.StockTransferKeys']] = None,
        relations: Optional[Mapping['types.StockTransferRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.StockTransferKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _StockTransfer_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _StockTransfer_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _StockTransfer_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _StockTransfer_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _StockTransfer_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _StockTransfer_relational_fields:
                        raise errors.UnknownRelationalFieldError('StockTransfer', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid StockTransfer / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'StockTransfer',
            }
        )
        _created_partial_types.add(name)


class StockTransferLine(bases.BaseStockTransferLine):
    """Represents a StockTransferLine record"""

    id: _str
    stockTransferId: _str
    productCode: _str
    quantity: decimal.Decimal
    unitCost: decimal.Decimal
    stockTransfer: Optional['models.StockTransfer'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.StockTransferLineKeys']] = None,
        exclude: Optional[Iterable['types.StockTransferLineKeys']] = None,
        required: Optional[Iterable['types.StockTransferLineKeys']] = None,
        optional: Optional[Iterable['types.StockTransferLineKeys']] = None,
        relations: Optional[Mapping['types.StockTransferLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.StockTransferLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _StockTransferLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _StockTransferLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _StockTransferLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _StockTransferLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _StockTransferLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _StockTransferLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('StockTransferLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid StockTransferLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'StockTransferLine',
            }
        )
        _created_partial_types.add(name)


class ProductBatch(bases.BaseProductBatch):
    """Product batch + expiry â€” catalog Â§7.8.
    """

    id: _str
    companyId: _str
    productCode: _str
    batchNumber: _str
    expiryDate: Optional[datetime.datetime] = None
    quantityOnHand: decimal.Decimal
    notes: Optional[_str] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductBatchKeys']] = None,
        exclude: Optional[Iterable['types.ProductBatchKeys']] = None,
        required: Optional[Iterable['types.ProductBatchKeys']] = None,
        optional: Optional[Iterable['types.ProductBatchKeys']] = None,
        relations: Optional[Mapping['types.ProductBatchRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductBatchKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProductBatch_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProductBatch_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProductBatch_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProductBatch_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProductBatch_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProductBatch_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProductBatch', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProductBatch / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProductBatch',
            }
        )
        _created_partial_types.add(name)


class DeliveryNote(bases.BaseDeliveryNote):
    """Delivery Note â€” sales side (GDNSI/GDNSO). Catalog Â§5.6.
    """

    id: _str
    companyId: _str
    voucherNumber: _str
    deliveryDate: datetime.datetime
    customerId: _str
    customerCode: Optional[_str] = None
    customerName: Optional[_str] = None
    sourceKind: _str
    """"GDNSI" linked from invoice, "GDNSO" linked from sales order.
    """

    sourceId: Optional[_str] = None
    status: _str
    notes: Optional[_str] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    lines: Optional[List['models.DeliveryNoteLine']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DeliveryNoteKeys']] = None,
        exclude: Optional[Iterable['types.DeliveryNoteKeys']] = None,
        required: Optional[Iterable['types.DeliveryNoteKeys']] = None,
        optional: Optional[Iterable['types.DeliveryNoteKeys']] = None,
        relations: Optional[Mapping['types.DeliveryNoteRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DeliveryNoteKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _DeliveryNote_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _DeliveryNote_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _DeliveryNote_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _DeliveryNote_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _DeliveryNote_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _DeliveryNote_relational_fields:
                        raise errors.UnknownRelationalFieldError('DeliveryNote', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid DeliveryNote / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'DeliveryNote',
            }
        )
        _created_partial_types.add(name)


class DeliveryNoteLine(bases.BaseDeliveryNoteLine):
    """Represents a DeliveryNoteLine record"""

    id: _str
    deliveryNoteId: _str
    productCode: Optional[_str] = None
    quantity: decimal.Decimal
    notes: Optional[_str] = None
    deliveryNote: Optional['models.DeliveryNote'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DeliveryNoteLineKeys']] = None,
        exclude: Optional[Iterable['types.DeliveryNoteLineKeys']] = None,
        required: Optional[Iterable['types.DeliveryNoteLineKeys']] = None,
        optional: Optional[Iterable['types.DeliveryNoteLineKeys']] = None,
        relations: Optional[Mapping['types.DeliveryNoteLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DeliveryNoteLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _DeliveryNoteLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _DeliveryNoteLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _DeliveryNoteLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _DeliveryNoteLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _DeliveryNoteLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _DeliveryNoteLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('DeliveryNoteLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid DeliveryNoteLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'DeliveryNoteLine',
            }
        )
        _created_partial_types.add(name)


class GoodsReceiptNote(bases.BaseGoodsReceiptNote):
    """Goods Receipt Note â€” purchases side (GRNPO/GRNVI). Catalog Â§6.
    """

    id: _str
    companyId: _str
    voucherNumber: _str
    receiptDate: datetime.datetime
    supplierId: _str
    supplierCode: Optional[_str] = None
    supplierName: Optional[_str] = None
    sourceKind: _str
    """"GRNPO" linked from purchase order, "GRNVI" linked from supplier bill.
    """

    sourceId: Optional[_str] = None
    status: _str
    notes: Optional[_str] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    lines: Optional[List['models.GoodsReceiptNoteLine']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.GoodsReceiptNoteKeys']] = None,
        exclude: Optional[Iterable['types.GoodsReceiptNoteKeys']] = None,
        required: Optional[Iterable['types.GoodsReceiptNoteKeys']] = None,
        optional: Optional[Iterable['types.GoodsReceiptNoteKeys']] = None,
        relations: Optional[Mapping['types.GoodsReceiptNoteRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.GoodsReceiptNoteKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _GoodsReceiptNote_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _GoodsReceiptNote_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _GoodsReceiptNote_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _GoodsReceiptNote_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _GoodsReceiptNote_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _GoodsReceiptNote_relational_fields:
                        raise errors.UnknownRelationalFieldError('GoodsReceiptNote', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid GoodsReceiptNote / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'GoodsReceiptNote',
            }
        )
        _created_partial_types.add(name)


class GoodsReceiptNoteLine(bases.BaseGoodsReceiptNoteLine):
    """Represents a GoodsReceiptNoteLine record"""

    id: _str
    goodsReceiptNoteId: _str
    productCode: Optional[_str] = None
    quantity: decimal.Decimal
    notes: Optional[_str] = None
    goodsReceiptNote: Optional['models.GoodsReceiptNote'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.GoodsReceiptNoteLineKeys']] = None,
        exclude: Optional[Iterable['types.GoodsReceiptNoteLineKeys']] = None,
        required: Optional[Iterable['types.GoodsReceiptNoteLineKeys']] = None,
        optional: Optional[Iterable['types.GoodsReceiptNoteLineKeys']] = None,
        relations: Optional[Mapping['types.GoodsReceiptNoteLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.GoodsReceiptNoteLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _GoodsReceiptNoteLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _GoodsReceiptNoteLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _GoodsReceiptNoteLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _GoodsReceiptNoteLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _GoodsReceiptNoteLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _GoodsReceiptNoteLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('GoodsReceiptNoteLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid GoodsReceiptNoteLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'GoodsReceiptNoteLine',
            }
        )
        _created_partial_types.add(name)


class PostDatedChequeReceived(bases.BasePostDatedChequeReceived):
    """Post-Dated Cheque Received (PDCR) â€” catalog Â§5.7.
    """

    id: _str
    companyId: _str
    voucherNumber: _str
    chequeNumber: _str
    bankName: _str
    customerId: _str
    customerCode: Optional[_str] = None
    customerName: Optional[_str] = None
    receivedDate: datetime.datetime
    chequeDate: datetime.datetime
    amount: decimal.Decimal
    status: _str
    """"pending" | "presented" | "cleared" | "bounced" | "cancelled"
    """

    linkedReceiptId: Optional[_str] = None
    """SalesReceipt id created on clearing (null until cleared).
    """

    notes: Optional[_str] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PostDatedChequeReceivedKeys']] = None,
        exclude: Optional[Iterable['types.PostDatedChequeReceivedKeys']] = None,
        required: Optional[Iterable['types.PostDatedChequeReceivedKeys']] = None,
        optional: Optional[Iterable['types.PostDatedChequeReceivedKeys']] = None,
        relations: Optional[Mapping['types.PostDatedChequeReceivedRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PostDatedChequeReceivedKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _PostDatedChequeReceived_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _PostDatedChequeReceived_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _PostDatedChequeReceived_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _PostDatedChequeReceived_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _PostDatedChequeReceived_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _PostDatedChequeReceived_relational_fields:
                        raise errors.UnknownRelationalFieldError('PostDatedChequeReceived', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid PostDatedChequeReceived / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'PostDatedChequeReceived',
            }
        )
        _created_partial_types.add(name)


class PostDatedChequeIssued(bases.BasePostDatedChequeIssued):
    """Post-Dated Cheque Issued (PDCI) â€” catalog Â§6.1.
    """

    id: _str
    companyId: _str
    voucherNumber: _str
    chequeNumber: _str
    bankAccountId: _str
    supplierId: _str
    supplierCode: Optional[_str] = None
    supplierName: Optional[_str] = None
    issuedDate: datetime.datetime
    chequeDate: datetime.datetime
    amount: decimal.Decimal
    status: _str
    linkedPaymentId: Optional[_str] = None
    notes: Optional[_str] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PostDatedChequeIssuedKeys']] = None,
        exclude: Optional[Iterable['types.PostDatedChequeIssuedKeys']] = None,
        required: Optional[Iterable['types.PostDatedChequeIssuedKeys']] = None,
        optional: Optional[Iterable['types.PostDatedChequeIssuedKeys']] = None,
        relations: Optional[Mapping['types.PostDatedChequeIssuedRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PostDatedChequeIssuedKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _PostDatedChequeIssued_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _PostDatedChequeIssued_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _PostDatedChequeIssued_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _PostDatedChequeIssued_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _PostDatedChequeIssued_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _PostDatedChequeIssued_relational_fields:
                        raise errors.UnknownRelationalFieldError('PostDatedChequeIssued', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid PostDatedChequeIssued / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'PostDatedChequeIssued',
            }
        )
        _created_partial_types.add(name)


class GoodsIssue(bases.BaseGoodsIssue):
    """Stock issue against a posted sales invoice â€” P2 goods issue (catalog Â§5.4 / Â§13.1).
    """

    id: _str
    companyId: _str
    voucherNumber: _str
    issueDate: datetime.datetime
    salesInvoiceId: _str
    status: _str
    journalId: Optional[_str] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    lines: Optional[List['models.GoodsIssueLine']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.GoodsIssueKeys']] = None,
        exclude: Optional[Iterable['types.GoodsIssueKeys']] = None,
        required: Optional[Iterable['types.GoodsIssueKeys']] = None,
        optional: Optional[Iterable['types.GoodsIssueKeys']] = None,
        relations: Optional[Mapping['types.GoodsIssueRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.GoodsIssueKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _GoodsIssue_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _GoodsIssue_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _GoodsIssue_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _GoodsIssue_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _GoodsIssue_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _GoodsIssue_relational_fields:
                        raise errors.UnknownRelationalFieldError('GoodsIssue', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid GoodsIssue / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'GoodsIssue',
            }
        )
        _created_partial_types.add(name)


class GoodsIssueLine(bases.BaseGoodsIssueLine):
    """Represents a GoodsIssueLine record"""

    id: _str
    goodsIssueId: _str
    productCode: _str
    quantity: decimal.Decimal
    unitCost: decimal.Decimal
    batchNumber: Optional[_str] = None
    goodsIssue: Optional['models.GoodsIssue'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.GoodsIssueLineKeys']] = None,
        exclude: Optional[Iterable['types.GoodsIssueLineKeys']] = None,
        required: Optional[Iterable['types.GoodsIssueLineKeys']] = None,
        optional: Optional[Iterable['types.GoodsIssueLineKeys']] = None,
        relations: Optional[Mapping['types.GoodsIssueLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.GoodsIssueLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _GoodsIssueLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _GoodsIssueLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _GoodsIssueLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _GoodsIssueLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _GoodsIssueLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _GoodsIssueLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('GoodsIssueLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid GoodsIssueLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'GoodsIssueLine',
            }
        )
        _created_partial_types.add(name)


class AssemblyTemplate(bases.BaseAssemblyTemplate):
    """Represents a AssemblyTemplate record"""

    id: _str
    companyId: _str
    code: _str
    name: _str
    finishedProductCode: _str
    isActive: _bool
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    lines: Optional[List['models.AssemblyTemplateLine']] = None
    jobs: Optional[List['models.AssemblyJob']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.AssemblyTemplateKeys']] = None,
        exclude: Optional[Iterable['types.AssemblyTemplateKeys']] = None,
        required: Optional[Iterable['types.AssemblyTemplateKeys']] = None,
        optional: Optional[Iterable['types.AssemblyTemplateKeys']] = None,
        relations: Optional[Mapping['types.AssemblyTemplateRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.AssemblyTemplateKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _AssemblyTemplate_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _AssemblyTemplate_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _AssemblyTemplate_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _AssemblyTemplate_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _AssemblyTemplate_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _AssemblyTemplate_relational_fields:
                        raise errors.UnknownRelationalFieldError('AssemblyTemplate', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid AssemblyTemplate / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'AssemblyTemplate',
            }
        )
        _created_partial_types.add(name)


class AssemblyTemplateLine(bases.BaseAssemblyTemplateLine):
    """Represents a AssemblyTemplateLine record"""

    id: _str
    templateId: _str
    componentProductCode: _str
    quantity: decimal.Decimal
    template: Optional['models.AssemblyTemplate'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.AssemblyTemplateLineKeys']] = None,
        exclude: Optional[Iterable['types.AssemblyTemplateLineKeys']] = None,
        required: Optional[Iterable['types.AssemblyTemplateLineKeys']] = None,
        optional: Optional[Iterable['types.AssemblyTemplateLineKeys']] = None,
        relations: Optional[Mapping['types.AssemblyTemplateLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.AssemblyTemplateLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _AssemblyTemplateLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _AssemblyTemplateLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _AssemblyTemplateLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _AssemblyTemplateLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _AssemblyTemplateLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _AssemblyTemplateLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('AssemblyTemplateLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid AssemblyTemplateLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'AssemblyTemplateLine',
            }
        )
        _created_partial_types.add(name)


class AssemblyJob(bases.BaseAssemblyJob):
    """Represents a AssemblyJob record"""

    id: _str
    companyId: _str
    jobNumber: _str
    jobDate: datetime.datetime
    templateId: Optional[_str] = None
    finishedProductCode: _str
    quantity: decimal.Decimal
    status: _str
    journalId: Optional[_str] = None
    batchNumber: Optional[_str] = None
    expiryDate: Optional[datetime.datetime] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None
    template: Optional['models.AssemblyTemplate'] = None
    lines: Optional[List['models.AssemblyJobLine']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.AssemblyJobKeys']] = None,
        exclude: Optional[Iterable['types.AssemblyJobKeys']] = None,
        required: Optional[Iterable['types.AssemblyJobKeys']] = None,
        optional: Optional[Iterable['types.AssemblyJobKeys']] = None,
        relations: Optional[Mapping['types.AssemblyJobRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.AssemblyJobKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _AssemblyJob_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _AssemblyJob_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _AssemblyJob_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _AssemblyJob_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _AssemblyJob_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _AssemblyJob_relational_fields:
                        raise errors.UnknownRelationalFieldError('AssemblyJob', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid AssemblyJob / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'AssemblyJob',
            }
        )
        _created_partial_types.add(name)


class AssemblyJobLine(bases.BaseAssemblyJobLine):
    """Represents a AssemblyJobLine record"""

    id: _str
    jobId: _str
    componentProductCode: _str
    quantity: decimal.Decimal
    unitCost: decimal.Decimal
    job: Optional['models.AssemblyJob'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.AssemblyJobLineKeys']] = None,
        exclude: Optional[Iterable['types.AssemblyJobLineKeys']] = None,
        required: Optional[Iterable['types.AssemblyJobLineKeys']] = None,
        optional: Optional[Iterable['types.AssemblyJobLineKeys']] = None,
        relations: Optional[Mapping['types.AssemblyJobLineRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.AssemblyJobLineKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _AssemblyJobLine_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _AssemblyJobLine_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _AssemblyJobLine_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _AssemblyJobLine_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _AssemblyJobLine_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _AssemblyJobLine_relational_fields:
                        raise errors.UnknownRelationalFieldError('AssemblyJobLine', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid AssemblyJobLine / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'AssemblyJobLine',
            }
        )
        _created_partial_types.add(name)


class FxRevaluationRun(bases.BaseFxRevaluationRun):
    """Represents a FxRevaluationRun record"""

    id: _str
    companyId: _str
    revaluationDate: datetime.datetime
    bankAccountId: _str
    foreignBalance: decimal.Decimal
    exchangeRate: decimal.Decimal
    gainLossAmount: decimal.Decimal
    journalId: Optional[_str] = None
    status: _str
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.FxRevaluationRunKeys']] = None,
        exclude: Optional[Iterable['types.FxRevaluationRunKeys']] = None,
        required: Optional[Iterable['types.FxRevaluationRunKeys']] = None,
        optional: Optional[Iterable['types.FxRevaluationRunKeys']] = None,
        relations: Optional[Mapping['types.FxRevaluationRunRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.FxRevaluationRunKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _FxRevaluationRun_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _FxRevaluationRun_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _FxRevaluationRun_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _FxRevaluationRun_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _FxRevaluationRun_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _FxRevaluationRun_relational_fields:
                        raise errors.UnknownRelationalFieldError('FxRevaluationRun', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid FxRevaluationRun / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'FxRevaluationRun',
            }
        )
        _created_partial_types.add(name)


class DigitalInvoiceSubmission(bases.BaseDigitalInvoiceSubmission):
    """Represents a DigitalInvoiceSubmission record"""

    id: _str
    companyId: _str
    salesInvoiceId: _str
    status: _str
    fbrReference: Optional[_str] = None
    requestPayload: 'fields.Json'
    responsePayload: Optional['fields.Json'] = None
    submittedAt: Optional[datetime.datetime] = None
    retryCount: _int
    lastError: Optional[_str] = None
    nextRetryAt: Optional[datetime.datetime] = None
    createdAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DigitalInvoiceSubmissionKeys']] = None,
        exclude: Optional[Iterable['types.DigitalInvoiceSubmissionKeys']] = None,
        required: Optional[Iterable['types.DigitalInvoiceSubmissionKeys']] = None,
        optional: Optional[Iterable['types.DigitalInvoiceSubmissionKeys']] = None,
        relations: Optional[Mapping['types.DigitalInvoiceSubmissionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DigitalInvoiceSubmissionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _DigitalInvoiceSubmission_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _DigitalInvoiceSubmission_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _DigitalInvoiceSubmission_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _DigitalInvoiceSubmission_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _DigitalInvoiceSubmission_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _DigitalInvoiceSubmission_relational_fields:
                        raise errors.UnknownRelationalFieldError('DigitalInvoiceSubmission', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid DigitalInvoiceSubmission / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'DigitalInvoiceSubmission',
            }
        )
        _created_partial_types.add(name)


class PaymentGatewayTransaction(bases.BasePaymentGatewayTransaction):
    """Online payment provider events (PayPro / Kuickpay) â€” P5.
    """

    id: _str
    companyId: _str
    provider: _str
    merchantRef: _str
    amount: decimal.Decimal
    status: _str
    customerId: Optional[_str] = None
    salesReceiptId: Optional[_str] = None
    payload: 'fields.Json'
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PaymentGatewayTransactionKeys']] = None,
        exclude: Optional[Iterable['types.PaymentGatewayTransactionKeys']] = None,
        required: Optional[Iterable['types.PaymentGatewayTransactionKeys']] = None,
        optional: Optional[Iterable['types.PaymentGatewayTransactionKeys']] = None,
        relations: Optional[Mapping['types.PaymentGatewayTransactionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PaymentGatewayTransactionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _PaymentGatewayTransaction_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _PaymentGatewayTransaction_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _PaymentGatewayTransaction_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _PaymentGatewayTransaction_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _PaymentGatewayTransaction_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _PaymentGatewayTransaction_relational_fields:
                        raise errors.UnknownRelationalFieldError('PaymentGatewayTransaction', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid PaymentGatewayTransaction / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'PaymentGatewayTransaction',
            }
        )
        _created_partial_types.add(name)


class DocumentTaxSummary(bases.BaseDocumentTaxSummary):
    """Tax fields are added inline to SalesInvoiceLine / SupplierBillLine / etc.
    New table tracks the tax-on-document totals for fast roll-ups.
    """

    id: _str
    companyId: _str
    documentKind: _str
    """"SI" / "SC" / "VI" / "VC"
    """

    documentId: _str
    taxCode: _str
    """"gst" / "adt" / "fed" / "wht"
    """

    taxRate: decimal.Decimal
    taxBase: decimal.Decimal
    taxAmount: decimal.Decimal
    nominalCode: Optional[_str] = None
    createdAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DocumentTaxSummaryKeys']] = None,
        exclude: Optional[Iterable['types.DocumentTaxSummaryKeys']] = None,
        required: Optional[Iterable['types.DocumentTaxSummaryKeys']] = None,
        optional: Optional[Iterable['types.DocumentTaxSummaryKeys']] = None,
        relations: Optional[Mapping['types.DocumentTaxSummaryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DocumentTaxSummaryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _DocumentTaxSummary_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _DocumentTaxSummary_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _DocumentTaxSummary_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _DocumentTaxSummary_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "DocumentTaxSummary" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid DocumentTaxSummary / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'DocumentTaxSummary',
            }
        )
        _created_partial_types.add(name)



_Company_relational_fields: Set[str] = {
        'memberships',
        'roles',
        'businessInformation',
        'smartSettings',
        'lockDateSettings',
        'taxesYearEnd',
        'coaCategories',
        'bankAccounts',
        'customers',
        'suppliers',
        'products',
        'projects',
        'locations',
        'journals',
        'attachments',
        'importJobs',
        'auditLogEntries',
        'documentSequences',
        'approvalPolicies',
        'salesInvoices',
        'supplierBills',
        'bankPayments',
        'salesReceipts',
        'supplierPayments',
        'bankReceipts',
        'bankTransfers',
        'quotations',
        'salesOrders',
        'purchaseOrders',
        'salesCredits',
        'supplierCredits',
        'stockAdjustments',
        'stockTransfers',
        'productBatches',
        'deliveryNotes',
        'goodsReceiptNotes',
        'pdcReceived',
        'pdcIssued',
        'outboxEvents',
        'reportRuns',
        'bankReconciliations',
        'goodsIssues',
        'approvalRequests',
        'assemblyTemplates',
        'assemblyJobs',
        'moduleEntitlements',
        'customFieldDefinitions',
        'subscriptionRecord',
        'fxRevaluationRuns',
        'digitalInvoiceSubmissions',
        'paymentGatewayTransactions',
        'userOnboardingStates',
        'onboardingReleaseItems',
        'assistantThreads',
        'budgets',
        'transactionTemplates',
        'recurringSchedules',
        'customerAdvanceReturns',
        'supplierAdvanceReturns',
        'companyModuleConfigs',
        'fieldSecurityPolicies',
        'userDataAssignments',
    }
_Company_fields: Dict['types.CompanyKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('memberships', {
            'name': 'memberships',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.CompanyMembership\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('roles', {
            'name': 'roles',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Role\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('businessInformation', {
            'name': 'businessInformation',
            'is_list': False,
            'optional': True,
            'type': 'models.BusinessInformation',
            'is_relational': True,
            'documentation': None,
        }),
        ('smartSettings', {
            'name': 'smartSettings',
            'is_list': False,
            'optional': True,
            'type': 'models.SmartSettings',
            'is_relational': True,
            'documentation': None,
        }),
        ('lockDateSettings', {
            'name': 'lockDateSettings',
            'is_list': False,
            'optional': True,
            'type': 'models.LockDateSettings',
            'is_relational': True,
            'documentation': None,
        }),
        ('taxesYearEnd', {
            'name': 'taxesYearEnd',
            'is_list': False,
            'optional': True,
            'type': 'models.TaxesYearEndConfig',
            'is_relational': True,
            'documentation': None,
        }),
        ('coaCategories', {
            'name': 'coaCategories',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.CoaCategory\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('bankAccounts', {
            'name': 'bankAccounts',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.BankAccount\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('customers', {
            'name': 'customers',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Customer\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('suppliers', {
            'name': 'suppliers',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Supplier\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('products', {
            'name': 'products',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Product\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('projects', {
            'name': 'projects',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Project\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('locations', {
            'name': 'locations',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Location\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('journals', {
            'name': 'journals',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Journal\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('attachments', {
            'name': 'attachments',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Attachment\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('importJobs', {
            'name': 'importJobs',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ImportJob\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('auditLogEntries', {
            'name': 'auditLogEntries',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.AuditLogEntry\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('documentSequences', {
            'name': 'documentSequences',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.DocumentNumberSequence\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('approvalPolicies', {
            'name': 'approvalPolicies',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ApprovalPolicy\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('salesInvoices', {
            'name': 'salesInvoices',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SalesInvoice\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('supplierBills', {
            'name': 'supplierBills',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SupplierBill\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('bankPayments', {
            'name': 'bankPayments',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.BankPayment\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('salesReceipts', {
            'name': 'salesReceipts',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SalesReceipt\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('supplierPayments', {
            'name': 'supplierPayments',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SupplierPayment\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('bankReceipts', {
            'name': 'bankReceipts',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.BankReceipt\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('bankTransfers', {
            'name': 'bankTransfers',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.BankTransfer\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('quotations', {
            'name': 'quotations',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Quotation\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('salesOrders', {
            'name': 'salesOrders',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SalesOrder\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('purchaseOrders', {
            'name': 'purchaseOrders',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.PurchaseOrder\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('salesCredits', {
            'name': 'salesCredits',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SalesCredit\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('supplierCredits', {
            'name': 'supplierCredits',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SupplierCredit\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('stockAdjustments', {
            'name': 'stockAdjustments',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.StockAdjustment\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('stockTransfers', {
            'name': 'stockTransfers',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.StockTransfer\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('productBatches', {
            'name': 'productBatches',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductBatch\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('deliveryNotes', {
            'name': 'deliveryNotes',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.DeliveryNote\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('goodsReceiptNotes', {
            'name': 'goodsReceiptNotes',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.GoodsReceiptNote\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('pdcReceived', {
            'name': 'pdcReceived',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.PostDatedChequeReceived\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('pdcIssued', {
            'name': 'pdcIssued',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.PostDatedChequeIssued\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('outboxEvents', {
            'name': 'outboxEvents',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.OutboxEvent\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('reportRuns', {
            'name': 'reportRuns',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ReportRun\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('bankReconciliations', {
            'name': 'bankReconciliations',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.BankReconciliation\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('goodsIssues', {
            'name': 'goodsIssues',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.GoodsIssue\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('approvalRequests', {
            'name': 'approvalRequests',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ApprovalRequest\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('assemblyTemplates', {
            'name': 'assemblyTemplates',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.AssemblyTemplate\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('assemblyJobs', {
            'name': 'assemblyJobs',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.AssemblyJob\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('moduleEntitlements', {
            'name': 'moduleEntitlements',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.CompanyModuleEntitlement\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('customFieldDefinitions', {
            'name': 'customFieldDefinitions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.CustomFieldDefinition\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('subscriptionRecord', {
            'name': 'subscriptionRecord',
            'is_list': False,
            'optional': True,
            'type': 'models.SubscriptionRecord',
            'is_relational': True,
            'documentation': None,
        }),
        ('fxRevaluationRuns', {
            'name': 'fxRevaluationRuns',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FxRevaluationRun\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('digitalInvoiceSubmissions', {
            'name': 'digitalInvoiceSubmissions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.DigitalInvoiceSubmission\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('paymentGatewayTransactions', {
            'name': 'paymentGatewayTransactions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.PaymentGatewayTransaction\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('userOnboardingStates', {
            'name': 'userOnboardingStates',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.UserOnboardingState\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('onboardingReleaseItems', {
            'name': 'onboardingReleaseItems',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.OnboardingReleaseItem\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('assistantThreads', {
            'name': 'assistantThreads',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.AssistantThread\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('budgets', {
            'name': 'budgets',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Budget\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('transactionTemplates', {
            'name': 'transactionTemplates',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.TransactionTemplate\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('recurringSchedules', {
            'name': 'recurringSchedules',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.RecurringSchedule\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('customerAdvanceReturns', {
            'name': 'customerAdvanceReturns',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.CustomerAdvanceReturn\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('supplierAdvanceReturns', {
            'name': 'supplierAdvanceReturns',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SupplierAdvanceReturn\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('companyModuleConfigs', {
            'name': 'companyModuleConfigs',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.CompanyModuleConfig\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('fieldSecurityPolicies', {
            'name': 'fieldSecurityPolicies',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FieldSecurityPolicy\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('userDataAssignments', {
            'name': 'userDataAssignments',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.UserDataAssignment\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_AssistantThread_relational_fields: Set[str] = {
        'company',
        'user',
        'messages',
    }
_AssistantThread_fields: Dict['types.AssistantThreadKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('userId', {
            'name': 'userId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('locale', {
            'name': 'locale',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('user', {
            'name': 'user',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
        ('messages', {
            'name': 'messages',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.AssistantMessage\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_AssistantMessage_relational_fields: Set[str] = {
        'thread',
    }
_AssistantMessage_fields: Dict['types.AssistantMessageKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('threadId', {
            'name': 'threadId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('role', {
            'name': 'role',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('content', {
            'name': 'content',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('toolName', {
            'name': 'toolName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('toolPayload', {
            'name': 'toolPayload',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('thread', {
            'name': 'thread',
            'is_list': False,
            'optional': True,
            'type': 'models.AssistantThread',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_PlatformOnboardingRelease_relational_fields: Set[str] = set()  # pyright: ignore[reportUnusedVariable]
_PlatformOnboardingRelease_fields: Dict['types.PlatformOnboardingReleaseKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('releaseKey', {
            'name': 'releaseKey',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('version', {
            'name': 'version',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('title', {
            'name': 'title',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('summary', {
            'name': 'summary',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('publishedAt', {
            'name': 'publishedAt',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('tourId', {
            'name': 'tourId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('href', {
            'name': 'href',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('permissions', {
            'name': 'permissions',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('isActive', {
            'name': 'isActive',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('sortOrder', {
            'name': 'sortOrder',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_OnboardingReleaseItem_relational_fields: Set[str] = {
        'company',
    }
_OnboardingReleaseItem_fields: Dict['types.OnboardingReleaseItemKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('releaseKey', {
            'name': 'releaseKey',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('version', {
            'name': 'version',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('title', {
            'name': 'title',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('summary', {
            'name': 'summary',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('publishedAt', {
            'name': 'publishedAt',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('tourId', {
            'name': 'tourId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('href', {
            'name': 'href',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('permissions', {
            'name': 'permissions',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('isActive', {
            'name': 'isActive',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('sortOrder', {
            'name': 'sortOrder',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_UserOnboardingState_relational_fields: Set[str] = {
        'company',
        'user',
    }
_UserOnboardingState_fields: Dict['types.UserOnboardingStateKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('userId', {
            'name': 'userId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('payload', {
            'name': 'payload',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('user', {
            'name': 'user',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_User_relational_fields: Set[str] = {
        'memberships',
        'lockDatePerUserRows',
        'otpChallenges',
        'onboardingStates',
        'assistantThreads',
    }
_User_fields: Dict['types.UserKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('email', {
            'name': 'email',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('passwordHash', {
            'name': 'passwordHash',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('firstName', {
            'name': 'firstName',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('lastName', {
            'name': 'lastName',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('phone', {
            'name': 'phone',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isActive', {
            'name': 'isActive',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('emailVerified', {
            'name': 'emailVerified',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': '''When false, user must complete ``signup`` email OTP before login.''',
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('memberships', {
            'name': 'memberships',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.CompanyMembership\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('lockDatePerUserRows', {
            'name': 'lockDatePerUserRows',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.LockDatePerUser\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('otpChallenges', {
            'name': 'otpChallenges',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.AuthOtpChallenge\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('onboardingStates', {
            'name': 'onboardingStates',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.UserOnboardingState\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('assistantThreads', {
            'name': 'assistantThreads',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.AssistantThread\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_AuthOtpChallenge_relational_fields: Set[str] = {
        'user',
    }
_AuthOtpChallenge_fields: Dict['types.AuthOtpChallengeKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('userId', {
            'name': 'userId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('purpose', {
            'name': 'purpose',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('codeHash', {
            'name': 'codeHash',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('expiresAt', {
            'name': 'expiresAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('user', {
            'name': 'user',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_CompanyMembership_relational_fields: Set[str] = {
        'company',
        'user',
        'role',
        'membershipRoles',
        'dataAssignments',
    }
_CompanyMembership_fields: Dict['types.CompanyMembershipKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('userId', {
            'name': 'userId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('roleId', {
            'name': 'roleId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isDefault', {
            'name': 'isDefault',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('ipAllowlist', {
            'name': 'ipAllowlist',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('user', {
            'name': 'user',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
        ('role', {
            'name': 'role',
            'is_list': False,
            'optional': True,
            'type': 'models.Role',
            'is_relational': True,
            'documentation': None,
        }),
        ('membershipRoles', {
            'name': 'membershipRoles',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.MembershipRole\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('dataAssignments', {
            'name': 'dataAssignments',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.UserDataAssignment\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Role_relational_fields: Set[str] = {
        'company',
        'parentRole',
        'childRoles',
        'memberships',
        'membershipRoles',
        'rolePermissions',
        'fieldSecurityPolicies',
    }
_Role_fields: Dict['types.RoleKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('code', {
            'name': 'code',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('parentRoleId', {
            'name': 'parentRoleId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isSystem', {
            'name': 'isSystem',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('isTemplate', {
            'name': 'isTemplate',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('sortOrder', {
            'name': 'sortOrder',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('permissions', {
            'name': 'permissions',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('parentRole', {
            'name': 'parentRole',
            'is_list': False,
            'optional': True,
            'type': 'models.Role',
            'is_relational': True,
            'documentation': None,
        }),
        ('childRoles', {
            'name': 'childRoles',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Role\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('memberships', {
            'name': 'memberships',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.CompanyMembership\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('membershipRoles', {
            'name': 'membershipRoles',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.MembershipRole\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('rolePermissions', {
            'name': 'rolePermissions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.RolePermission\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('fieldSecurityPolicies', {
            'name': 'fieldSecurityPolicies',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FieldSecurityPolicy\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_PermissionDefinition_relational_fields: Set[str] = {
        'rolePermissions',
    }
_PermissionDefinition_fields: Dict['types.PermissionDefinitionKeys', PartialModelField] = OrderedDict(
    [
        ('code', {
            'name': 'code',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('module', {
            'name': 'module',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('resource', {
            'name': 'resource',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('action', {
            'name': 'action',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('label', {
            'name': 'label',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('groupLabel', {
            'name': 'groupLabel',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sortOrder', {
            'name': 'sortOrder',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('rolePermissions', {
            'name': 'rolePermissions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.RolePermission\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_RolePermission_relational_fields: Set[str] = {
        'role',
        'permission',
    }
_RolePermission_fields: Dict['types.RolePermissionKeys', PartialModelField] = OrderedDict(
    [
        ('roleId', {
            'name': 'roleId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('permissionCode', {
            'name': 'permissionCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('granted', {
            'name': 'granted',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('role', {
            'name': 'role',
            'is_list': False,
            'optional': True,
            'type': 'models.Role',
            'is_relational': True,
            'documentation': None,
        }),
        ('permission', {
            'name': 'permission',
            'is_list': False,
            'optional': True,
            'type': 'models.PermissionDefinition',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_MembershipRole_relational_fields: Set[str] = {
        'membership',
        'role',
    }
_MembershipRole_fields: Dict['types.MembershipRoleKeys', PartialModelField] = OrderedDict(
    [
        ('membershipId', {
            'name': 'membershipId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('roleId', {
            'name': 'roleId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isPrimary', {
            'name': 'isPrimary',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('membership', {
            'name': 'membership',
            'is_list': False,
            'optional': True,
            'type': 'models.CompanyMembership',
            'is_relational': True,
            'documentation': None,
        }),
        ('role', {
            'name': 'role',
            'is_list': False,
            'optional': True,
            'type': 'models.Role',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_CompanyModuleConfig_relational_fields: Set[str] = {
        'company',
    }
_CompanyModuleConfig_fields: Dict['types.CompanyModuleConfigKeys', PartialModelField] = OrderedDict(
    [
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('moduleCode', {
            'name': 'moduleCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('enabled', {
            'name': 'enabled',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('sidebarVisible', {
            'name': 'sidebarVisible',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('routesEnabled', {
            'name': 'routesEnabled',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('apiEnabled', {
            'name': 'apiEnabled',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('reportsEnabled', {
            'name': 'reportsEnabled',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('widgetsEnabled', {
            'name': 'widgetsEnabled',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_FieldSecurityPolicy_relational_fields: Set[str] = {
        'company',
        'role',
    }
_FieldSecurityPolicy_fields: Dict['types.FieldSecurityPolicyKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('roleId', {
            'name': 'roleId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('fieldKey', {
            'name': 'fieldKey',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('accessLevel', {
            'name': 'accessLevel',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('role', {
            'name': 'role',
            'is_list': False,
            'optional': True,
            'type': 'models.Role',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_UserDataAssignment_relational_fields: Set[str] = {
        'company',
        'membership',
    }
_UserDataAssignment_fields: Dict['types.UserDataAssignmentKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('membershipId', {
            'name': 'membershipId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('scopeType', {
            'name': 'scopeType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('scopeId', {
            'name': 'scopeId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('membership', {
            'name': 'membership',
            'is_list': False,
            'optional': True,
            'type': 'models.CompanyMembership',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_BusinessInformation_relational_fields: Set[str] = {
        'company',
    }
_BusinessInformation_fields: Dict['types.BusinessInformationKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('businessName', {
            'name': 'businessName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('addressLine1', {
            'name': 'addressLine1',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('addressLine2', {
            'name': 'addressLine2',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('addressLine3', {
            'name': 'addressLine3',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('addressLine4', {
            'name': 'addressLine4',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('addressLine5', {
            'name': 'addressLine5',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('branchName', {
            'name': 'branchName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('phoneNumber', {
            'name': 'phoneNumber',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('mobileNumber', {
            'name': 'mobileNumber',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('emailAddress', {
            'name': 'emailAddress',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('websiteAddress', {
            'name': 'websiteAddress',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('logoUrl', {
            'name': 'logoUrl',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('cnic', {
            'name': 'cnic',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('stn', {
            'name': 'stn',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('ntn', {
            'name': 'ntn',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('applyOnAllPrints', {
            'name': 'applyOnAllPrints',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SmartSettings_relational_fields: Set[str] = {
        'company',
    }
_SmartSettings_fields: Dict['types.SmartSettingsKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('payload', {
            'name': 'payload',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_LockDateSettings_relational_fields: Set[str] = {
        'company',
        'perUserExtensions',
    }
_LockDateSettings_fields: Dict['types.LockDateSettingsKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('globalLockDate', {
            'name': 'globalLockDate',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('perUserExtensions', {
            'name': 'perUserExtensions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.LockDatePerUser\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_LockDatePerUser_relational_fields: Set[str] = {
        'lockDateSettings',
        'user',
    }
_LockDatePerUser_fields: Dict['types.LockDatePerUserKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('lockDateSettingsId', {
            'name': 'lockDateSettingsId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('userId', {
            'name': 'userId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('extendedLockDate', {
            'name': 'extendedLockDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('lockDateSettings', {
            'name': 'lockDateSettings',
            'is_list': False,
            'optional': True,
            'type': 'models.LockDateSettings',
            'is_relational': True,
            'documentation': None,
        }),
        ('user', {
            'name': 'user',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_TaxesYearEndConfig_relational_fields: Set[str] = {
        'company',
    }
_TaxesYearEndConfig_fields: Dict['types.TaxesYearEndConfigKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('yearEndDate', {
            'name': 'yearEndDate',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxDisplay', {
            'name': 'taxDisplay',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('gstRates', {
            'name': 'gstRates',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('fedRates', {
            'name': 'fedRates',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('adtRates', {
            'name': 'adtRates',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('whtRates', {
            'name': 'whtRates',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxRegions', {
            'name': 'taxRegions',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_CoaCategory_relational_fields: Set[str] = {
        'company',
        'sections',
    }
_CoaCategory_fields: Dict['types.CoaCategoryKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('code', {
            'name': 'code',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sortOrder', {
            'name': 'sortOrder',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('categoryType', {
            'name': 'categoryType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Income / Expense / Asset / Liability / Equity / Other â€” drives P&L vs Balance Sheet classification.''',
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('sections', {
            'name': 'sections',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.CoaSection\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_CoaSection_relational_fields: Set[str] = {
        'category',
        'nominals',
    }
_CoaSection_fields: Dict['types.CoaSectionKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('categoryId', {
            'name': 'categoryId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('code', {
            'name': 'code',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sortOrder', {
            'name': 'sortOrder',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('category', {
            'name': 'category',
            'is_list': False,
            'optional': True,
            'type': 'models.CoaCategory',
            'is_relational': True,
            'documentation': None,
        }),
        ('nominals', {
            'name': 'nominals',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.NominalAccount\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_NominalAccount_relational_fields: Set[str] = {
        'section',
        'journalLines',
    }
_NominalAccount_fields: Dict['types.NominalAccountKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sectionId', {
            'name': 'sectionId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('code', {
            'name': 'code',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isChargeDeduction', {
            'name': 'isChargeDeduction',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('section', {
            'name': 'section',
            'is_list': False,
            'optional': True,
            'type': 'models.CoaSection',
            'is_relational': True,
            'documentation': None,
        }),
        ('journalLines', {
            'name': 'journalLines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.JournalLine\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_BankAccount_relational_fields: Set[str] = {
        'company',
    }
_BankAccount_fields: Dict['types.BankAccountKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('nominalCode', {
            'name': 'nominalCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('currency', {
            'name': 'currency',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isActive', {
            'name': 'isActive',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Customer_relational_fields: Set[str] = {
        'company',
    }
_Customer_fields: Dict['types.CustomerKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('code', {
            'name': 'code',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('email', {
            'name': 'email',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('phone', {
            'name': 'phone',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customFields', {
            'name': 'customFields',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Supplier_relational_fields: Set[str] = {
        'company',
    }
_Supplier_fields: Dict['types.SupplierKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('code', {
            'name': 'code',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('email', {
            'name': 'email',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('phone', {
            'name': 'phone',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Product_relational_fields: Set[str] = {
        'company',
        'uoms',
    }
_Product_fields: Dict['types.ProductKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('code', {
            'name': 'code',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('category', {
            'name': 'category',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('unit', {
            'name': 'unit',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('cost', {
            'name': 'cost',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('salePrice', {
            'name': 'salePrice',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('isStock', {
            'name': 'isStock',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('isArchived', {
            'name': 'isArchived',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('customFields', {
            'name': 'customFields',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('uoms', {
            'name': 'uoms',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductUom\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_CompanyModuleEntitlement_relational_fields: Set[str] = {
        'company',
    }
_CompanyModuleEntitlement_fields: Dict['types.CompanyModuleEntitlementKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('moduleCode', {
            'name': 'moduleCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('enabled', {
            'name': 'enabled',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_CustomFieldDefinition_relational_fields: Set[str] = {
        'company',
    }
_CustomFieldDefinition_fields: Dict['types.CustomFieldDefinitionKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('entityType', {
            'name': 'entityType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('fieldKey', {
            'name': 'fieldKey',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('label', {
            'name': 'label',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('fieldType', {
            'name': 'fieldType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isRequired', {
            'name': 'isRequired',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('picklistOptions', {
            'name': 'picklistOptions',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SubscriptionRecord_relational_fields: Set[str] = {
        'company',
    }
_SubscriptionRecord_fields: Dict['types.SubscriptionRecordKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('planCode', {
            'name': 'planCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('externalCustomerId', {
            'name': 'externalCustomerId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('currentPeriodEnd', {
            'name': 'currentPeriodEnd',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('metadata', {
            'name': 'metadata',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ProductUom_relational_fields: Set[str] = {
        'product',
    }
_ProductUom_fields: Dict['types.ProductUomKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('unitCode', {
            'name': 'unitCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('conversionFactor', {
            'name': 'conversionFactor',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('salePrice', {
            'name': 'salePrice',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('isDefault', {
            'name': 'isDefault',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Project_relational_fields: Set[str] = {
        'company',
    }
_Project_fields: Dict['types.ProjectKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('code', {
            'name': 'code',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isActive', {
            'name': 'isActive',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Location_relational_fields: Set[str] = {
        'company',
    }
_Location_fields: Dict['types.LocationKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('code', {
            'name': 'code',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isMain', {
            'name': 'isMain',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Journal_relational_fields: Set[str] = {
        'company',
        'lines',
    }
_Journal_fields: Dict['types.JournalKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('journalNumber', {
            'name': 'journalNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('journalDate', {
            'name': 'journalDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('refNo', {
            'name': 'refNo',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('totalAmount', {
            'name': 'totalAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sourceType', {
            'name': 'sourceType',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''e.g. SALES_INVOICE, STOCK_ADJUSTMENT â€” P0 traceability''',
        }),
        ('sourceId', {
            'name': 'sourceId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('correlationId', {
            'name': 'correlationId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('reversesJournalId', {
            'name': 'reversesJournalId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.JournalLine\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_JournalLine_relational_fields: Set[str] = {
        'journal',
        'nominalAccount',
    }
_JournalLine_fields: Dict['types.JournalLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('journalId', {
            'name': 'journalId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('nominalCode', {
            'name': 'nominalCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('nominalAccountId', {
            'name': 'nominalAccountId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Optional FK to chart nominal (P3); ``nominalCode`` remains for legacy queries.''',
        }),
        ('debit', {
            'name': 'debit',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('credit', {
            'name': 'credit',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('projectCode', {
            'name': 'projectCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('journal', {
            'name': 'journal',
            'is_list': False,
            'optional': True,
            'type': 'models.Journal',
            'is_relational': True,
            'documentation': None,
        }),
        ('nominalAccount', {
            'name': 'nominalAccount',
            'is_list': False,
            'optional': True,
            'type': 'models.NominalAccount',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SalesInvoice_relational_fields: Set[str] = {
        'company',
        'lines',
        'allocations',
    }
_SalesInvoice_fields: Dict['types.SalesInvoiceKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('invoiceNumber', {
            'name': 'invoiceNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('invoiceDate', {
            'name': 'invoiceDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerId', {
            'name': 'customerId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerCode', {
            'name': 'customerCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Write-time snapshot (Step 3) â€” avoids JOIN to customers on reads.''',
        }),
        ('customerName', {
            'name': 'customerName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('totalAmount', {
            'name': 'totalAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('remainingAmount', {
            'name': 'remainingAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': '''Open balance after receipt allocations (Phase 3 denorm).''',
        }),
        ('journalId', {
            'name': 'journalId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''GL journal that posted this invoice (DR AR / CR Sales). Null when defaults missing at create time.''',
        }),
        ('customFields', {
            'name': 'customFields',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SalesInvoiceLine\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('allocations', {
            'name': 'allocations',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SalesReceiptAllocation\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SalesInvoiceLine_relational_fields: Set[str] = {
        'salesInvoice',
    }
_SalesInvoiceLine_fields: Dict['types.SalesInvoiceLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('salesInvoiceId', {
            'name': 'salesInvoiceId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productCode', {
            'name': 'productCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('rate', {
            'name': 'rate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('lineSubtotal', {
            'name': 'lineSubtotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': '''Net before tax (qty Ã— rate).''',
        }),
        ('gstCode', {
            'name': 'gstCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('gstRate', {
            'name': 'gstRate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxAmount', {
            'name': 'taxAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('lineTotal', {
            'name': 'lineTotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('projectCode', {
            'name': 'projectCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('descriptionFields', {
            'name': 'descriptionFields',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('batchNumber', {
            'name': 'batchNumber',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('expiryDate', {
            'name': 'expiryDate',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('salesInvoice', {
            'name': 'salesInvoice',
            'is_list': False,
            'optional': True,
            'type': 'models.SalesInvoice',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SupplierBill_relational_fields: Set[str] = {
        'company',
        'lines',
        'allocations',
    }
_SupplierBill_fields: Dict['types.SupplierBillKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('billNumber', {
            'name': 'billNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('billDate', {
            'name': 'billDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierId', {
            'name': 'supplierId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierCode', {
            'name': 'supplierCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Write-time snapshot (Step 3) â€” avoids JOIN to suppliers on reads.''',
        }),
        ('supplierName', {
            'name': 'supplierName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('totalAmount', {
            'name': 'totalAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('remainingAmount', {
            'name': 'remainingAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('journalId', {
            'name': 'journalId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''GL journal that posted this bill (DR Expense / CR AP). Null when defaults missing at create time.''',
        }),
        ('customFields', {
            'name': 'customFields',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SupplierBillLine\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('allocations', {
            'name': 'allocations',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SupplierPaymentAllocation\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SupplierBillLine_relational_fields: Set[str] = {
        'supplierBill',
    }
_SupplierBillLine_fields: Dict['types.SupplierBillLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierBillId', {
            'name': 'supplierBillId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productCode', {
            'name': 'productCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('rate', {
            'name': 'rate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('lineSubtotal', {
            'name': 'lineSubtotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('gstCode', {
            'name': 'gstCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('gstRate', {
            'name': 'gstRate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxAmount', {
            'name': 'taxAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('lineTotal', {
            'name': 'lineTotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('descriptionFields', {
            'name': 'descriptionFields',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('batchNumber', {
            'name': 'batchNumber',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('expiryDate', {
            'name': 'expiryDate',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierBill', {
            'name': 'supplierBill',
            'is_list': False,
            'optional': True,
            'type': 'models.SupplierBill',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_BankPayment_relational_fields: Set[str] = {
        'company',
        'customerAdvanceReturn',
    }
_BankPayment_fields: Dict['types.BankPaymentKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('voucherNumber', {
            'name': 'voucherNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('paymentDate', {
            'name': 'paymentDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('bankAccountId', {
            'name': 'bankAccountId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('nominalCode', {
            'name': 'nominalCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Counterpart nominal for the single-line journal (DR this code, CR bank account's nominal).
When null, the payment is recorded but no journal posts.''',
        }),
        ('totalAmount', {
            'name': 'totalAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('journalId', {
            'name': 'journalId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''GL journal that posted this payment.''',
        }),
        ('reconciledAt', {
            'name': 'reconciledAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('customFields', {
            'name': 'customFields',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('customerAdvanceReturn', {
            'name': 'customerAdvanceReturn',
            'is_list': False,
            'optional': True,
            'type': 'models.CustomerAdvanceReturn',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SalesReceipt_relational_fields: Set[str] = {
        'company',
        'allocations',
        'advanceReturns',
    }
_SalesReceipt_fields: Dict['types.SalesReceiptKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('receiptNumber', {
            'name': 'receiptNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('receiptDate', {
            'name': 'receiptDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerId', {
            'name': 'customerId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerCode', {
            'name': 'customerCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerName', {
            'name': 'customerName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('bankAccountId', {
            'name': 'bankAccountId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('totalAmount', {
            'name': 'totalAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('journalId', {
            'name': 'journalId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''GL journal: DR bank's nominal, CR receivables nominal.''',
        }),
        ('reconciledAt', {
            'name': 'reconciledAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('customFields', {
            'name': 'customFields',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('allocations', {
            'name': 'allocations',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SalesReceiptAllocation\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('advanceReturns', {
            'name': 'advanceReturns',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.CustomerAdvanceReturn\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SupplierPayment_relational_fields: Set[str] = {
        'company',
        'allocations',
        'advanceReturns',
    }
_SupplierPayment_fields: Dict['types.SupplierPaymentKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('voucherNumber', {
            'name': 'voucherNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('paymentDate', {
            'name': 'paymentDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierId', {
            'name': 'supplierId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierCode', {
            'name': 'supplierCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierName', {
            'name': 'supplierName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('bankAccountId', {
            'name': 'bankAccountId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('totalAmount', {
            'name': 'totalAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('journalId', {
            'name': 'journalId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''GL journal: DR payables nominal, CR bank's nominal.''',
        }),
        ('reconciledAt', {
            'name': 'reconciledAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('customFields', {
            'name': 'customFields',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('allocations', {
            'name': 'allocations',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SupplierPaymentAllocation\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('advanceReturns', {
            'name': 'advanceReturns',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SupplierAdvanceReturn\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SalesReceiptAllocation_relational_fields: Set[str] = {
        'salesReceipt',
        'salesInvoice',
    }
_SalesReceiptAllocation_fields: Dict['types.SalesReceiptAllocationKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('salesReceiptId', {
            'name': 'salesReceiptId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('salesInvoiceId', {
            'name': 'salesInvoiceId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('amount', {
            'name': 'amount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('salesReceipt', {
            'name': 'salesReceipt',
            'is_list': False,
            'optional': True,
            'type': 'models.SalesReceipt',
            'is_relational': True,
            'documentation': None,
        }),
        ('salesInvoice', {
            'name': 'salesInvoice',
            'is_list': False,
            'optional': True,
            'type': 'models.SalesInvoice',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SupplierPaymentAllocation_relational_fields: Set[str] = {
        'supplierPayment',
        'supplierBill',
    }
_SupplierPaymentAllocation_fields: Dict['types.SupplierPaymentAllocationKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierPaymentId', {
            'name': 'supplierPaymentId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierBillId', {
            'name': 'supplierBillId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('amount', {
            'name': 'amount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierPayment', {
            'name': 'supplierPayment',
            'is_list': False,
            'optional': True,
            'type': 'models.SupplierPayment',
            'is_relational': True,
            'documentation': None,
        }),
        ('supplierBill', {
            'name': 'supplierBill',
            'is_list': False,
            'optional': True,
            'type': 'models.SupplierBill',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_CustomerAdvanceReturn_relational_fields: Set[str] = {
        'company',
        'salesReceipt',
        'bankPayment',
    }
_CustomerAdvanceReturn_fields: Dict['types.CustomerAdvanceReturnKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('salesReceiptId', {
            'name': 'salesReceiptId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('bankPaymentId', {
            'name': 'bankPaymentId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('amount', {
            'name': 'amount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('returnDate', {
            'name': 'returnDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('salesReceipt', {
            'name': 'salesReceipt',
            'is_list': False,
            'optional': True,
            'type': 'models.SalesReceipt',
            'is_relational': True,
            'documentation': None,
        }),
        ('bankPayment', {
            'name': 'bankPayment',
            'is_list': False,
            'optional': True,
            'type': 'models.BankPayment',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SupplierAdvanceReturn_relational_fields: Set[str] = {
        'company',
        'supplierPayment',
        'bankReceipt',
    }
_SupplierAdvanceReturn_fields: Dict['types.SupplierAdvanceReturnKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierPaymentId', {
            'name': 'supplierPaymentId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('bankReceiptId', {
            'name': 'bankReceiptId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('amount', {
            'name': 'amount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('returnDate', {
            'name': 'returnDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('supplierPayment', {
            'name': 'supplierPayment',
            'is_list': False,
            'optional': True,
            'type': 'models.SupplierPayment',
            'is_relational': True,
            'documentation': None,
        }),
        ('bankReceipt', {
            'name': 'bankReceipt',
            'is_list': False,
            'optional': True,
            'type': 'models.BankReceipt',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_BankReceipt_relational_fields: Set[str] = {
        'company',
        'supplierAdvanceReturn',
    }
_BankReceipt_fields: Dict['types.BankReceiptKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('voucherNumber', {
            'name': 'voucherNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('receiptDate', {
            'name': 'receiptDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('bankAccountId', {
            'name': 'bankAccountId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('nominalCode', {
            'name': 'nominalCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Counterpart nominal (CR side) â€” typically an income or refund-control code.''',
        }),
        ('totalAmount', {
            'name': 'totalAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('journalId', {
            'name': 'journalId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''GL journal: DR bank's nominal, CR counterpart.''',
        }),
        ('reconciledAt', {
            'name': 'reconciledAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('customFields', {
            'name': 'customFields',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('supplierAdvanceReturn', {
            'name': 'supplierAdvanceReturn',
            'is_list': False,
            'optional': True,
            'type': 'models.SupplierAdvanceReturn',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Quotation_relational_fields: Set[str] = {
        'company',
        'lines',
    }
_Quotation_fields: Dict['types.QuotationKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quotationNumber', {
            'name': 'quotationNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quotationDate', {
            'name': 'quotationDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerId', {
            'name': 'customerId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerCode', {
            'name': 'customerCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerName', {
            'name': 'customerName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''draft | approved | rejected | accepted | converted''',
        }),
        ('totalAmount', {
            'name': 'totalAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.QuotationLine\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_QuotationLine_relational_fields: Set[str] = {
        'quotation',
    }
_QuotationLine_fields: Dict['types.QuotationLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quotationId', {
            'name': 'quotationId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productCode', {
            'name': 'productCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('rate', {
            'name': 'rate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('lineSubtotal', {
            'name': 'lineSubtotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('gstCode', {
            'name': 'gstCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('gstRate', {
            'name': 'gstRate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxAmount', {
            'name': 'taxAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('lineTotal', {
            'name': 'lineTotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('quotation', {
            'name': 'quotation',
            'is_list': False,
            'optional': True,
            'type': 'models.Quotation',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SalesOrder_relational_fields: Set[str] = {
        'company',
        'lines',
    }
_SalesOrder_fields: Dict['types.SalesOrderKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('orderNumber', {
            'name': 'orderNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('orderDate', {
            'name': 'orderDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerId', {
            'name': 'customerId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerCode', {
            'name': 'customerCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerName', {
            'name': 'customerName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''in_progress | approved | rejected | invoiced''',
        }),
        ('totalAmount', {
            'name': 'totalAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SalesOrderLine\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SalesOrderLine_relational_fields: Set[str] = {
        'salesOrder',
    }
_SalesOrderLine_fields: Dict['types.SalesOrderLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('salesOrderId', {
            'name': 'salesOrderId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productCode', {
            'name': 'productCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('rate', {
            'name': 'rate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('lineSubtotal', {
            'name': 'lineSubtotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('gstCode', {
            'name': 'gstCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('gstRate', {
            'name': 'gstRate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxAmount', {
            'name': 'taxAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('lineTotal', {
            'name': 'lineTotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('projectCode', {
            'name': 'projectCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('salesOrder', {
            'name': 'salesOrder',
            'is_list': False,
            'optional': True,
            'type': 'models.SalesOrder',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_PurchaseOrder_relational_fields: Set[str] = {
        'company',
        'lines',
    }
_PurchaseOrder_fields: Dict['types.PurchaseOrderKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('orderNumber', {
            'name': 'orderNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('orderDate', {
            'name': 'orderDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierId', {
            'name': 'supplierId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierCode', {
            'name': 'supplierCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierName', {
            'name': 'supplierName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''in_progress | approved | rejected | billed''',
        }),
        ('totalAmount', {
            'name': 'totalAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.PurchaseOrderLine\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_PurchaseOrderLine_relational_fields: Set[str] = {
        'purchaseOrder',
    }
_PurchaseOrderLine_fields: Dict['types.PurchaseOrderLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('purchaseOrderId', {
            'name': 'purchaseOrderId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productCode', {
            'name': 'productCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('rate', {
            'name': 'rate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('lineSubtotal', {
            'name': 'lineSubtotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('gstCode', {
            'name': 'gstCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('gstRate', {
            'name': 'gstRate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxAmount', {
            'name': 'taxAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('lineTotal', {
            'name': 'lineTotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('purchaseOrder', {
            'name': 'purchaseOrder',
            'is_list': False,
            'optional': True,
            'type': 'models.PurchaseOrder',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SalesCredit_relational_fields: Set[str] = {
        'company',
        'lines',
    }
_SalesCredit_fields: Dict['types.SalesCreditKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('creditNumber', {
            'name': 'creditNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('creditDate', {
            'name': 'creditDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerId', {
            'name': 'customerId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerCode', {
            'name': 'customerCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerName', {
            'name': 'customerName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('totalAmount', {
            'name': 'totalAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('journalId', {
            'name': 'journalId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SalesCreditLine\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SalesCreditLine_relational_fields: Set[str] = {
        'salesCredit',
    }
_SalesCreditLine_fields: Dict['types.SalesCreditLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('salesCreditId', {
            'name': 'salesCreditId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productCode', {
            'name': 'productCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('rate', {
            'name': 'rate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('lineSubtotal', {
            'name': 'lineSubtotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('gstCode', {
            'name': 'gstCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('gstRate', {
            'name': 'gstRate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxAmount', {
            'name': 'taxAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('lineTotal', {
            'name': 'lineTotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('salesCredit', {
            'name': 'salesCredit',
            'is_list': False,
            'optional': True,
            'type': 'models.SalesCredit',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SupplierCredit_relational_fields: Set[str] = {
        'company',
        'lines',
    }
_SupplierCredit_fields: Dict['types.SupplierCreditKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('creditNumber', {
            'name': 'creditNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('creditDate', {
            'name': 'creditDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierId', {
            'name': 'supplierId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierCode', {
            'name': 'supplierCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierName', {
            'name': 'supplierName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('totalAmount', {
            'name': 'totalAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('journalId', {
            'name': 'journalId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SupplierCreditLine\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_SupplierCreditLine_relational_fields: Set[str] = {
        'supplierCredit',
    }
_SupplierCreditLine_fields: Dict['types.SupplierCreditLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierCreditId', {
            'name': 'supplierCreditId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productCode', {
            'name': 'productCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('rate', {
            'name': 'rate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('lineSubtotal', {
            'name': 'lineSubtotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('gstCode', {
            'name': 'gstCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('gstRate', {
            'name': 'gstRate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxAmount', {
            'name': 'taxAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('lineTotal', {
            'name': 'lineTotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierCredit', {
            'name': 'supplierCredit',
            'is_list': False,
            'optional': True,
            'type': 'models.SupplierCredit',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_BankTransfer_relational_fields: Set[str] = {
        'company',
    }
_BankTransfer_fields: Dict['types.BankTransferKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('voucherNumber', {
            'name': 'voucherNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('transferDate', {
            'name': 'transferDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('fromBankAccountId', {
            'name': 'fromBankAccountId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('toBankAccountId', {
            'name': 'toBankAccountId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('totalAmount', {
            'name': 'totalAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('journalId', {
            'name': 'journalId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''GL journal: DR to-bank nominal, CR from-bank nominal.''',
        }),
        ('reconciledAt', {
            'name': 'reconciledAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Attachment_relational_fields: Set[str] = {
        'company',
    }
_Attachment_fields: Dict['types.AttachmentKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('entityType', {
            'name': 'entityType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('entityId', {
            'name': 'entityId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('fileName', {
            'name': 'fileName',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('storageKey', {
            'name': 'storageKey',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('mimeType', {
            'name': 'mimeType',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('byteSize', {
            'name': 'byteSize',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_DocumentNumberSequence_relational_fields: Set[str] = {
        'company',
    }
_DocumentNumberSequence_fields: Dict['types.DocumentNumberSequenceKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('key', {
            'name': 'key',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('nextValue', {
            'name': 'nextValue',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ImportJob_relational_fields: Set[str] = {
        'company',
    }
_ImportJob_fields: Dict['types.ImportJobKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('jobType', {
            'name': 'jobType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('fileName', {
            'name': 'fileName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('payload', {
            'name': 'payload',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''Inline rows for API-driven imports (CSV parsed client-side or JSON upload).''',
        }),
        ('errorCount', {
            'name': 'errorCount',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('resultSummary', {
            'name': 'resultSummary',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_TransactionTemplate_relational_fields: Set[str] = {
        'company',
    }
_TransactionTemplate_fields: Dict['types.TransactionTemplateKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('module', {
            'name': 'module',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''e.g. sales_invoice, supplier_bill, bank_payment''',
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('payload', {
            'name': 'payload',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdBy', {
            'name': 'createdBy',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_RecurringSchedule_relational_fields: Set[str] = {
        'company',
    }
_RecurringSchedule_fields: Dict['types.RecurringScheduleKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('module', {
            'name': 'module',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''sales_invoice, supplier_bill, bank_payment, journal''',
        }),
        ('frequency', {
            'name': 'frequency',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('interval', {
            'name': 'interval',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('nextRunDate', {
            'name': 'nextRunDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('endDate', {
            'name': 'endDate',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('isActive', {
            'name': 'isActive',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('payload', {
            'name': 'payload',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('lastRunAt', {
            'name': 'lastRunAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_OutboxEvent_relational_fields: Set[str] = {
        'company',
    }
_OutboxEvent_fields: Dict['types.OutboxEventKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('eventType', {
            'name': 'eventType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('payload', {
            'name': 'payload',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('attempts', {
            'name': 'attempts',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('lastError', {
            'name': 'lastError',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('lockedAt', {
            'name': 'lockedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('nextAttemptAt', {
            'name': 'nextAttemptAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('processedAt', {
            'name': 'processedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ReportRun_relational_fields: Set[str] = {
        'company',
    }
_ReportRun_fields: Dict['types.ReportRunKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('reportId', {
            'name': 'reportId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('criteria', {
            'name': 'criteria',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('rowCount', {
            'name': 'rowCount',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('rows', {
            'name': 'rows',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('storageKey', {
            'name': 'storageKey',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Filesystem or object-store path when row payload is stored externally (Phase 2).''',
        }),
        ('errorMessage', {
            'name': 'errorMessage',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('completedAt', {
            'name': 'completedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_BankReconciliation_relational_fields: Set[str] = {
        'company',
        'items',
    }
_BankReconciliation_fields: Dict['types.BankReconciliationKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('bankAccountId', {
            'name': 'bankAccountId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('statementDate', {
            'name': 'statementDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('statementBalance', {
            'name': 'statementBalance',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('notes', {
            'name': 'notes',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('completedAt', {
            'name': 'completedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('items', {
            'name': 'items',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.BankReconciliationItem\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_BankReconciliationItem_relational_fields: Set[str] = {
        'reconciliation',
    }
_BankReconciliationItem_fields: Dict['types.BankReconciliationItemKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('reconciliationId', {
            'name': 'reconciliationId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('itemType', {
            'name': 'itemType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('itemId', {
            'name': 'itemId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('transactionDate', {
            'name': 'transactionDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('amount', {
            'name': 'amount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('reference', {
            'name': 'reference',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isCleared', {
            'name': 'isCleared',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('reconciliation', {
            'name': 'reconciliation',
            'is_list': False,
            'optional': True,
            'type': 'models.BankReconciliation',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_AuditLogEntry_relational_fields: Set[str] = {
        'company',
    }
_AuditLogEntry_fields: Dict['types.AuditLogEntryKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('userId', {
            'name': 'userId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('transactionType', {
            'name': 'transactionType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('transactionId', {
            'name': 'transactionId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('details', {
            'name': 'details',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ApprovalPolicy_relational_fields: Set[str] = {
        'company',
    }
_ApprovalPolicy_fields: Dict['types.ApprovalPolicyKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('entityType', {
            'name': 'entityType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('rules', {
            'name': 'rules',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ApprovalRequest_relational_fields: Set[str] = {
        'company',
    }
_ApprovalRequest_fields: Dict['types.ApprovalRequestKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('entityType', {
            'name': 'entityType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('entityId', {
            'name': 'entityId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('amount', {
            'name': 'amount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('requestedById', {
            'name': 'requestedById',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('approvedById', {
            'name': 'approvedById',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('approvedAt', {
            'name': 'approvedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('notes', {
            'name': 'notes',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Budget_relational_fields: Set[str] = {
        'company',
        'lines',
    }
_Budget_fields: Dict['types.BudgetKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('code', {
            'name': 'code',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('fiscalYear', {
            'name': 'fiscalYear',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('isActive', {
            'name': 'isActive',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.BudgetLine\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_BudgetLine_relational_fields: Set[str] = {
        'budget',
    }
_BudgetLine_fields: Dict['types.BudgetLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('budgetId', {
            'name': 'budgetId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('nominalCode', {
            'name': 'nominalCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('period', {
            'name': 'period',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('amount', {
            'name': 'amount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('budget', {
            'name': 'budget',
            'is_list': False,
            'optional': True,
            'type': 'models.Budget',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_StockAdjustment_relational_fields: Set[str] = {
        'company',
        'lines',
    }
_StockAdjustment_fields: Dict['types.StockAdjustmentKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('voucherNumber', {
            'name': 'voucherNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('adjustmentDate', {
            'name': 'adjustmentDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('reason', {
            'name': 'reason',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"increase" | "decrease" | "writeoff"''',
        }),
        ('notes', {
            'name': 'notes',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('journalId', {
            'name': 'journalId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.StockAdjustmentLine\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_StockAdjustmentLine_relational_fields: Set[str] = {
        'stockAdjustment',
    }
_StockAdjustmentLine_fields: Dict['types.StockAdjustmentLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('stockAdjustmentId', {
            'name': 'stockAdjustmentId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productCode', {
            'name': 'productCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('locationCode', {
            'name': 'locationCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantityDelta', {
            'name': 'quantityDelta',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': '''Positive = increase, negative = decrease.''',
        }),
        ('unitCost', {
            'name': 'unitCost',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('stockAdjustment', {
            'name': 'stockAdjustment',
            'is_list': False,
            'optional': True,
            'type': 'models.StockAdjustment',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_StockTransfer_relational_fields: Set[str] = {
        'company',
        'lines',
    }
_StockTransfer_fields: Dict['types.StockTransferKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('voucherNumber', {
            'name': 'voucherNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('transferDate', {
            'name': 'transferDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('fromLocationCode', {
            'name': 'fromLocationCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('toLocationCode', {
            'name': 'toLocationCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('notes', {
            'name': 'notes',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.StockTransferLine\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_StockTransferLine_relational_fields: Set[str] = {
        'stockTransfer',
    }
_StockTransferLine_fields: Dict['types.StockTransferLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('stockTransferId', {
            'name': 'stockTransferId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productCode', {
            'name': 'productCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('unitCost', {
            'name': 'unitCost',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('stockTransfer', {
            'name': 'stockTransfer',
            'is_list': False,
            'optional': True,
            'type': 'models.StockTransfer',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ProductBatch_relational_fields: Set[str] = {
        'company',
    }
_ProductBatch_fields: Dict['types.ProductBatchKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productCode', {
            'name': 'productCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('batchNumber', {
            'name': 'batchNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('expiryDate', {
            'name': 'expiryDate',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantityOnHand', {
            'name': 'quantityOnHand',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('notes', {
            'name': 'notes',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_DeliveryNote_relational_fields: Set[str] = {
        'company',
        'lines',
    }
_DeliveryNote_fields: Dict['types.DeliveryNoteKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('voucherNumber', {
            'name': 'voucherNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('deliveryDate', {
            'name': 'deliveryDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerId', {
            'name': 'customerId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerCode', {
            'name': 'customerCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerName', {
            'name': 'customerName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sourceKind', {
            'name': 'sourceKind',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"GDNSI" linked from invoice, "GDNSO" linked from sales order.''',
        }),
        ('sourceId', {
            'name': 'sourceId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('notes', {
            'name': 'notes',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.DeliveryNoteLine\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_DeliveryNoteLine_relational_fields: Set[str] = {
        'deliveryNote',
    }
_DeliveryNoteLine_fields: Dict['types.DeliveryNoteLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('deliveryNoteId', {
            'name': 'deliveryNoteId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productCode', {
            'name': 'productCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('notes', {
            'name': 'notes',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('deliveryNote', {
            'name': 'deliveryNote',
            'is_list': False,
            'optional': True,
            'type': 'models.DeliveryNote',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_GoodsReceiptNote_relational_fields: Set[str] = {
        'company',
        'lines',
    }
_GoodsReceiptNote_fields: Dict['types.GoodsReceiptNoteKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('voucherNumber', {
            'name': 'voucherNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('receiptDate', {
            'name': 'receiptDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierId', {
            'name': 'supplierId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierCode', {
            'name': 'supplierCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierName', {
            'name': 'supplierName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sourceKind', {
            'name': 'sourceKind',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"GRNPO" linked from purchase order, "GRNVI" linked from supplier bill.''',
        }),
        ('sourceId', {
            'name': 'sourceId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('notes', {
            'name': 'notes',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.GoodsReceiptNoteLine\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_GoodsReceiptNoteLine_relational_fields: Set[str] = {
        'goodsReceiptNote',
    }
_GoodsReceiptNoteLine_fields: Dict['types.GoodsReceiptNoteLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('goodsReceiptNoteId', {
            'name': 'goodsReceiptNoteId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productCode', {
            'name': 'productCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('notes', {
            'name': 'notes',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('goodsReceiptNote', {
            'name': 'goodsReceiptNote',
            'is_list': False,
            'optional': True,
            'type': 'models.GoodsReceiptNote',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_PostDatedChequeReceived_relational_fields: Set[str] = {
        'company',
    }
_PostDatedChequeReceived_fields: Dict['types.PostDatedChequeReceivedKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('voucherNumber', {
            'name': 'voucherNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('chequeNumber', {
            'name': 'chequeNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('bankName', {
            'name': 'bankName',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerId', {
            'name': 'customerId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerCode', {
            'name': 'customerCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerName', {
            'name': 'customerName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('receivedDate', {
            'name': 'receivedDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('chequeDate', {
            'name': 'chequeDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('amount', {
            'name': 'amount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"pending" | "presented" | "cleared" | "bounced" | "cancelled"''',
        }),
        ('linkedReceiptId', {
            'name': 'linkedReceiptId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''SalesReceipt id created on clearing (null until cleared).''',
        }),
        ('notes', {
            'name': 'notes',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_PostDatedChequeIssued_relational_fields: Set[str] = {
        'company',
    }
_PostDatedChequeIssued_fields: Dict['types.PostDatedChequeIssuedKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('voucherNumber', {
            'name': 'voucherNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('chequeNumber', {
            'name': 'chequeNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('bankAccountId', {
            'name': 'bankAccountId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierId', {
            'name': 'supplierId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierCode', {
            'name': 'supplierCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supplierName', {
            'name': 'supplierName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('issuedDate', {
            'name': 'issuedDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('chequeDate', {
            'name': 'chequeDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('amount', {
            'name': 'amount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('linkedPaymentId', {
            'name': 'linkedPaymentId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('notes', {
            'name': 'notes',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_GoodsIssue_relational_fields: Set[str] = {
        'company',
        'lines',
    }
_GoodsIssue_fields: Dict['types.GoodsIssueKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('voucherNumber', {
            'name': 'voucherNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('issueDate', {
            'name': 'issueDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('salesInvoiceId', {
            'name': 'salesInvoiceId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('journalId', {
            'name': 'journalId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.GoodsIssueLine\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_GoodsIssueLine_relational_fields: Set[str] = {
        'goodsIssue',
    }
_GoodsIssueLine_fields: Dict['types.GoodsIssueLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('goodsIssueId', {
            'name': 'goodsIssueId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productCode', {
            'name': 'productCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('unitCost', {
            'name': 'unitCost',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('batchNumber', {
            'name': 'batchNumber',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('goodsIssue', {
            'name': 'goodsIssue',
            'is_list': False,
            'optional': True,
            'type': 'models.GoodsIssue',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_AssemblyTemplate_relational_fields: Set[str] = {
        'company',
        'lines',
        'jobs',
    }
_AssemblyTemplate_fields: Dict['types.AssemblyTemplateKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('code', {
            'name': 'code',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('finishedProductCode', {
            'name': 'finishedProductCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isActive', {
            'name': 'isActive',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.AssemblyTemplateLine\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('jobs', {
            'name': 'jobs',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.AssemblyJob\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_AssemblyTemplateLine_relational_fields: Set[str] = {
        'template',
    }
_AssemblyTemplateLine_fields: Dict['types.AssemblyTemplateLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('templateId', {
            'name': 'templateId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('componentProductCode', {
            'name': 'componentProductCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('template', {
            'name': 'template',
            'is_list': False,
            'optional': True,
            'type': 'models.AssemblyTemplate',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_AssemblyJob_relational_fields: Set[str] = {
        'company',
        'template',
        'lines',
    }
_AssemblyJob_fields: Dict['types.AssemblyJobKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('jobNumber', {
            'name': 'jobNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('jobDate', {
            'name': 'jobDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('templateId', {
            'name': 'templateId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('finishedProductCode', {
            'name': 'finishedProductCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('journalId', {
            'name': 'journalId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('batchNumber', {
            'name': 'batchNumber',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('expiryDate', {
            'name': 'expiryDate',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
        ('template', {
            'name': 'template',
            'is_list': False,
            'optional': True,
            'type': 'models.AssemblyTemplate',
            'is_relational': True,
            'documentation': None,
        }),
        ('lines', {
            'name': 'lines',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.AssemblyJobLine\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_AssemblyJobLine_relational_fields: Set[str] = {
        'job',
    }
_AssemblyJobLine_fields: Dict['types.AssemblyJobLineKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('jobId', {
            'name': 'jobId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('componentProductCode', {
            'name': 'componentProductCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('unitCost', {
            'name': 'unitCost',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('job', {
            'name': 'job',
            'is_list': False,
            'optional': True,
            'type': 'models.AssemblyJob',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_FxRevaluationRun_relational_fields: Set[str] = {
        'company',
    }
_FxRevaluationRun_fields: Dict['types.FxRevaluationRunKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('revaluationDate', {
            'name': 'revaluationDate',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('bankAccountId', {
            'name': 'bankAccountId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('foreignBalance', {
            'name': 'foreignBalance',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('exchangeRate', {
            'name': 'exchangeRate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('gainLossAmount', {
            'name': 'gainLossAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('journalId', {
            'name': 'journalId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_DigitalInvoiceSubmission_relational_fields: Set[str] = {
        'company',
    }
_DigitalInvoiceSubmission_fields: Dict['types.DigitalInvoiceSubmissionKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('salesInvoiceId', {
            'name': 'salesInvoiceId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('fbrReference', {
            'name': 'fbrReference',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('requestPayload', {
            'name': 'requestPayload',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('responsePayload', {
            'name': 'responsePayload',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('submittedAt', {
            'name': 'submittedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('retryCount', {
            'name': 'retryCount',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('lastError', {
            'name': 'lastError',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('nextRetryAt', {
            'name': 'nextRetryAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_PaymentGatewayTransaction_relational_fields: Set[str] = {
        'company',
    }
_PaymentGatewayTransaction_fields: Dict['types.PaymentGatewayTransactionKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('provider', {
            'name': 'provider',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('merchantRef', {
            'name': 'merchantRef',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('amount', {
            'name': 'amount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerId', {
            'name': 'customerId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('salesReceiptId', {
            'name': 'salesReceiptId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('payload', {
            'name': 'payload',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_DocumentTaxSummary_relational_fields: Set[str] = set()  # pyright: ignore[reportUnusedVariable]
_DocumentTaxSummary_fields: Dict['types.DocumentTaxSummaryKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('companyId', {
            'name': 'companyId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('documentKind', {
            'name': 'documentKind',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"SI" / "SC" / "VI" / "VC"''',
        }),
        ('documentId', {
            'name': 'documentId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxCode', {
            'name': 'taxCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"gst" / "adt" / "fed" / "wht"''',
        }),
        ('taxRate', {
            'name': 'taxRate',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxBase', {
            'name': 'taxBase',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxAmount', {
            'name': 'taxAmount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('nominalCode', {
            'name': 'nominalCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)



# we have to import ourselves as relation types are namespaced to models
# e.g. models.Post
from . import models, actions

# required to support relationships between models
model_rebuild(Company)
model_rebuild(AssistantThread)
model_rebuild(AssistantMessage)
model_rebuild(PlatformOnboardingRelease)
model_rebuild(OnboardingReleaseItem)
model_rebuild(UserOnboardingState)
model_rebuild(User)
model_rebuild(AuthOtpChallenge)
model_rebuild(CompanyMembership)
model_rebuild(Role)
model_rebuild(PermissionDefinition)
model_rebuild(RolePermission)
model_rebuild(MembershipRole)
model_rebuild(CompanyModuleConfig)
model_rebuild(FieldSecurityPolicy)
model_rebuild(UserDataAssignment)
model_rebuild(BusinessInformation)
model_rebuild(SmartSettings)
model_rebuild(LockDateSettings)
model_rebuild(LockDatePerUser)
model_rebuild(TaxesYearEndConfig)
model_rebuild(CoaCategory)
model_rebuild(CoaSection)
model_rebuild(NominalAccount)
model_rebuild(BankAccount)
model_rebuild(Customer)
model_rebuild(Supplier)
model_rebuild(Product)
model_rebuild(CompanyModuleEntitlement)
model_rebuild(CustomFieldDefinition)
model_rebuild(SubscriptionRecord)
model_rebuild(ProductUom)
model_rebuild(Project)
model_rebuild(Location)
model_rebuild(Journal)
model_rebuild(JournalLine)
model_rebuild(SalesInvoice)
model_rebuild(SalesInvoiceLine)
model_rebuild(SupplierBill)
model_rebuild(SupplierBillLine)
model_rebuild(BankPayment)
model_rebuild(SalesReceipt)
model_rebuild(SupplierPayment)
model_rebuild(SalesReceiptAllocation)
model_rebuild(SupplierPaymentAllocation)
model_rebuild(CustomerAdvanceReturn)
model_rebuild(SupplierAdvanceReturn)
model_rebuild(BankReceipt)
model_rebuild(Quotation)
model_rebuild(QuotationLine)
model_rebuild(SalesOrder)
model_rebuild(SalesOrderLine)
model_rebuild(PurchaseOrder)
model_rebuild(PurchaseOrderLine)
model_rebuild(SalesCredit)
model_rebuild(SalesCreditLine)
model_rebuild(SupplierCredit)
model_rebuild(SupplierCreditLine)
model_rebuild(BankTransfer)
model_rebuild(Attachment)
model_rebuild(DocumentNumberSequence)
model_rebuild(ImportJob)
model_rebuild(TransactionTemplate)
model_rebuild(RecurringSchedule)
model_rebuild(OutboxEvent)
model_rebuild(ReportRun)
model_rebuild(BankReconciliation)
model_rebuild(BankReconciliationItem)
model_rebuild(AuditLogEntry)
model_rebuild(ApprovalPolicy)
model_rebuild(ApprovalRequest)
model_rebuild(Budget)
model_rebuild(BudgetLine)
model_rebuild(StockAdjustment)
model_rebuild(StockAdjustmentLine)
model_rebuild(StockTransfer)
model_rebuild(StockTransferLine)
model_rebuild(ProductBatch)
model_rebuild(DeliveryNote)
model_rebuild(DeliveryNoteLine)
model_rebuild(GoodsReceiptNote)
model_rebuild(GoodsReceiptNoteLine)
model_rebuild(PostDatedChequeReceived)
model_rebuild(PostDatedChequeIssued)
model_rebuild(GoodsIssue)
model_rebuild(GoodsIssueLine)
model_rebuild(AssemblyTemplate)
model_rebuild(AssemblyTemplateLine)
model_rebuild(AssemblyJob)
model_rebuild(AssemblyJobLine)
model_rebuild(FxRevaluationRun)
model_rebuild(DigitalInvoiceSubmission)
model_rebuild(PaymentGatewayTransaction)
model_rebuild(DocumentTaxSummary)
